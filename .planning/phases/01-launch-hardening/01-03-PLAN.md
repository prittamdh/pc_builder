---
phase: 01-launch-hardening
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
owner: web-dev
files_modified:
  - src/services/compatibility_engine.py
  - src/services/compatibility_rules.py
  - src/domain/builder.py
  - src/static/app.js
  - src/static/style.css
  - tests/test_compatibility_rules.py
  - tests/test_frontend_e2e.py
autonomous: true
requirements: [FIT-01, FIT-02, FIT-03, WEB-03, WEB-04]
user_setup: []

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "For every rule in RULES, when both slots are selected and either value is missing (and the rule applies to that part), validate_build returns a CompatibilityWarning with level 'unverified' whose message names the part and the missing spec (FIT-01; one unit test per rule)"
    - "A GPU with a known length plus a case with no published GPU clearance gives exactly one unverified item naming the case and 'GPU clearance', and the verdict 'No problems found - 1 check unverified' (ROADMAP Success Criterion 1)"
    - "BuildSummary.verdict is always exactly one of 'Problems found', 'No problems found - N check(s) unverified' or 'All checks passed', and 'All checks passed' is never returned while unverified_count > 0 (FIT-02)"
    - "A None aio_radiator_mm on an air cooler, or a None height_mm on an AIO, is 'does not apply', not unverified; a cooler whose type is unknown is unverified (Pitfall 8)"
    - "When a selected CPU or GPU has no TDP, the response has an 'estimate' item naming that part and the typical wattage used, BuildSummary.wattage_notes lists it, and the UI's wattage line says it is an estimate (FIT-03)"
    - "The component picker (filter_candidates) still offers parts with missing data; only error-level failures filter candidates (regression guard)"
    - "Warning messages, including part names that come from retailer titles, are HTML-escaped before being inserted into the page"
    - "With REQUIRE_E2E=1, a missing Playwright install, a missing chromium or an app that fails to boot makes the e2e module FAIL instead of skip (WEB-03)"
    - "At a 375x812 viewport a user can search the catalog, open the builder picker, add a part, read the verdict and open compare, and the page has no horizontal overflow (WEB-04)"
    - "[EDGE FIT-01/adjacency] GPU length exactly equal to the case clearance (360 vs 360) passes; 361 vs 360 fails with the existing error"
    - "[EDGE FIT-01/empty] One slot empty gives no item for that rule; both slots filled with one value None gives one unverified item; both values None gives one unverified item naming both parts"
    - "[EDGE FIT-01/ordering] The warnings list is deterministic: RULES order, then the PSU capacity checks, then estimate notes; the same build returns an identical list every call"
    - "[EDGE FIT-02/adjacency] 0 problems and 0 unverified gives 'All checks passed'; 1 unverified gives 'No problems found - 1 check unverified'; 2 unverified gives 'No problems found - 2 checks unverified'; 1 error plus 3 unverified gives 'Problems found'"
    - "[EDGE FIT-02/empty] An empty build returns 'All checks passed' with unverified_count 0 (nothing selected, nothing unverified); the existing empty-build e2e test is updated to assert this text"
    - "[EDGE FIT-02/ordering] The verdict follows fixed precedence (problems, then unverified, then all passed), independent of the order of items in warnings"
    - "[EDGE FIT-03/empty] No CPU or GPU selected gives no wattage notes; both TDPs known gives no notes and no '(estimate)' marker"
    - "[EDGE FIT-03/encoding] Part names in notes are the stored product name, unmodified in JSON and HTML-escaped when rendered"
    - "[EDGE WEB-03/empty] With REQUIRE_E2E=1 and chromium unavailable, pytest reports an error or failure for the e2e module, not a skip"
    - "[EDGE WEB-03/adjacency] Any skip reason (browser missing, playwright missing, app did not start) counts as a failure under REQUIRE_E2E=1; without REQUIRE_E2E the old skip behavior stays for dev machines"
    - statement: "[EDGE WEB-03/ordering] Each e2e test uses a fresh page, so results don't depend on test order"
      verification: backstop
  prohibitions:
    - requirement_id: FIT-01
      category: values
      status: resolved
      verification: test
      flagged: true
      statement: "A missing spec must never be filled with a default, estimate or guessed value to make a fit check pass or fail; missing means unverified"
    - requirement_id: FIT-02
      category: values
      status: resolved
      verification: test
      flagged: true
      statement: "The UI must never show a pass-style verdict (green state, 'All checks passed', a checkmark) while any check is unverified, and must not hide unverified items behind a collapsed or de-emphasized control"
  artifacts:
    - path: src/services/compatibility_rules.py
      provides: "FIELD_LABELS, rule_applies(rule, view_a, view_b)"
      contains: "def rule_applies"
    - path: src/services/compatibility_engine.py
      provides: "unverified items, product_name on views, estimate notes"
      contains: "unverified"
    - path: src/domain/builder.py
      provides: "BuildSummary computed fields unverified_count, verdict, wattage_notes"
      contains: "computed_field"
    - path: src/static/app.js
      provides: "3-state verdict rendering, escaped warnings, estimate marker"
      contains: "summary.verdict"
    - path: tests/test_frontend_e2e.py
      provides: "REQUIRE_E2E guard, mobile_page fixture, 375px flow, unverified-verdict e2e"
      contains: "REQUIRE_E2E"
  key_links:
    - from: src/services/compatibility_engine.py
      to: src/services/compatibility_rules.py
      via: "validate_build calls rule_applies before emitting unverified"
      pattern: "rule_applies\\("
    - from: src/domain/builder.py
      to: src/services/builder_service.py
      via: "BuildSummary derives verdict/unverified_count/wattage_notes from the warnings list builder_service already passes, so builder_service.py (data-engineer owned) is untouched"
      pattern: "computed_field"
    - from: src/static/app.js
      to: /api/v1/builder/validate
      via: "validateBuild renders summary.verdict into #compatibility-status"
      pattern: "summary\\.verdict"
---

<objective>
Make fit checks honest (FIT-01..03) and prove the builder in a real browser at phone width (WEB-03, WEB-04). A missing spec becomes a named "unverified" item instead of a silent pass. The verdict has exactly three states. Default TDPs are called out as estimates. The Playwright suite can no longer pass by being skipped.

Purpose: ROADMAP Phase 1 Success Criteria 1 and 6. Core value: "When we don't know, we say so."
Output: engine, rules and schema changes; 3-state UI; extended unit tests; e2e guard plus mobile and unverified-verdict flows.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-launch-hardening/01-RESEARCH.md
@.planning/phases/01-launch-hardening/01-PATTERNS.md
@.claude/agents/web-dev.md
@src/services/compatibility_engine.py
@src/services/compatibility_rules.py
@src/domain/builder.py

Locked decision (PROJECT.md line 92, repeated in RESEARCH): "An unverified fit check is its own state, never shown as passed."

Ownership note: src/services/builder_service.py is data-engineer's. This plan deliberately does not edit it. validate_build keeps returning (compatible, warnings, estimated_wattage), and every new field on BuildSummary is a Pydantic computed_field derived from the warnings list builder_service already passes through. src/domain/builder.py counts as the "builder schemas" the ROADMAP assigns to web-dev.

Why sequential after 01-02: one shared working tree, and the full e2e suite boots the whole app. Running web-dev plans one at a time keeps a half-finished main.py from breaking this plan's browser run.
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): "unverified" from rule to engine to schema to sidebar, for all RULES</name>
  <files>src/services/compatibility_rules.py, src/services/compatibility_engine.py, src/domain/builder.py, src/static/app.js, tests/test_compatibility_rules.py</files>
  <read_first>
    - src/services/compatibility_engine.py (the file being modified; _merge, _group_selections, _eval_rule, validate_build, filter_candidates)
    - src/services/compatibility_rules.py (the file being modified; Rule dataclass, RULES, the comments on the cooler height and AIO radiator rules)
    - src/domain/builder.py (the file being modified; CompatibilityWarning, BuildSummary)
    - src/static/app.js (the file being modified; validateBuild at about lines 770-810, and the existing escapeHtml helper)
    - src/services/builder_service.py (read only: how BuildSummary is built; must stay untouched)
    - tests/test_compatibility_filtering.py (monkeypatch pattern for _group_selections with SimpleNamespace views, no DB)
    - .planning/phases/01-launch-hardening/01-RESEARCH.md (Pattern 5, the nullable-field table for all 9 rules, Pitfall 8)
  </read_first>
  <behavior>
    - One parametrized test per rule in RULES (9 cases): values set so one side is None, and the rule applies, gives exactly one item with level "unverified", whose message contains the missing part's product_name and that field's FIELD_LABELS label
    - GPU (product_name "Test GPU", length_mm 300) plus case (product_name "Test Case", max_gpu_length_mm None) gives a message containing "Test Case" and "GPU clearance", and verdict "No problems found - 1 check unverified"
    - Air cooler (cooler_type "Air", aio_radiator_mm None) plus a case with max_radiator_mm 360 gives no radiator unverified item; AIO cooler (cooler_type "AIO", height_mm None) gives no cooler-height unverified item; cooler_type None gives unverified for both
    - 360 vs 360 passes, 361 vs 360 gives the error
    - Both values None gives a single unverified item naming both parts
    - The verdict strings for counts 0, 1 and 2, and error plus unverified, match the must_haves truths exactly
    - An empty build gives "All checks passed"
    - filter_candidates keeps a candidate whose value is None; the existing tests/test_compatibility_filtering.py still passes
  </behavior>
  <action>
    Write the failing tests first. Monkeypatch CompatibilityEngine._group_selections to return SimpleNamespace views carrying product_name, the same approach tests/test_compatibility_filtering.py uses, so no database is touched.

    In src/services/compatibility_rules.py (per FIT-01):
    - Add FIELD_LABELS, a dict mapping every field used in RULES to a plain-English spec name. Examples: max_gpu_length_mm maps to "GPU clearance", length_mm to "length", supported_sockets to "supported sockets", height_mm to "height", max_cooler_height_mm to "CPU cooler clearance", aio_radiator_mm to "radiator size", max_radiator_mm to "radiator support", and form_factor to "form factor". Cover socket, memory_type, modules, memory_slots, capacity_gb and max_memory_gb too.
    - Add rule_applies(rule, view_a, view_b) -> bool, which encodes Pitfall 8 next to the existing rule comments. The cooler height rule does not apply when the cooler's cooler_type starts with "AIO". The AIO radiator rule does not apply when cooler_type is known and does not start with "AIO". Every other combination applies. An unknown cooler_type means "applies", so the result is honestly unverified.

    In src/services/compatibility_engine.py:
    - In _group_selections, after resolving each slot, set view.product_name = product.name for each (product, view) pair. _resolve_slot already returns one view per product, in order.
    - Keep _eval_rule's pass/fail logic unchanged, including returning None for missing values, so filter_candidates' error-only filtering is unaffected.
    - In validate_build's RULES loop, for each (item_a, item_b) pair: when rule_applies is False, skip the pair. When either value is None, append CompatibilityWarning(level="unverified", message=...). Otherwise call _eval_rule as today. The message names the part or parts missing the value and the FIELD_LABELS spec, in this format: "Unverified: <part name> has no published <label>, so <slot_a>/<slot_b> fit could not be checked." When both sides are missing, name both parts in one item.
    - Unverified items never set compatible to False.

    In src/domain/builder.py (per FIT-02):
    - Update the level comment to list "error", "warning", "unverified" and "estimate".
    - Add three pydantic computed_field properties to BuildSummary:
      - unverified_count: the count of warnings with level "unverified".
      - verdict: "Problems found" when any warning has level "error" or "warning". Otherwise, when unverified_count > 0, "No problems found - {n} check unverified" (singular for 1) or "- {n} checks unverified" (plural otherwise). Otherwise "All checks passed". Use an ASCII hyphen, exactly as FIT-02 writes it.
      - wattage_notes: the messages of warnings with level "estimate" (filled by Task 2).
    - Because they are computed, builder_service.py needs no change and they serialize in the /builder/validate response.

    In src/static/app.js, validateBuild:
    - Replace the two-way status branch with a three-way branch on summary.verdict. Set statusEl.innerText = summary.verdict. Set className to 'compatibility-status error' for "Problems found", 'compatibility-status unverified' when summary.unverified_count > 0, and otherwise 'compatibility-status ok'.
    - Change the warnings rendering so each message goes through the existing escapeHtml. Part names come from retailer titles, so unescaped innerHTML would be a stored-XSS sink.
  </action>
  <verify>
    <automated>python -m pytest -q tests/test_compatibility_rules.py tests/test_compatibility_filtering.py</automated>
    <fails_when>non-zero exit, or the summary line reports fewer than 20 passed</fails_when>
  </verify>
  <acceptance_criteria>
    - `python -m pytest -q tests/test_compatibility_rules.py tests/test_compatibility_filtering.py` exits 0
    - `python -m pytest -q tests/test_compatibility_rules.py -k unverified` reports at least 9 passed (one per rule)
    - `git grep -n "def rule_applies" -- src/services/compatibility_rules.py` prints one line
    - `git grep -n "computed_field" -- src/domain/builder.py` prints at least 3 lines
    - `git grep -n "summary.verdict" -- src/static/app.js` prints at least one line
    - `git grep -n 'escapeHtml(w.message)' -- src/static/app.js` prints one line
    - `git status --porcelain -- src/services/builder_service.py` prints nothing, and `git log --oneline <sha-before-this-plan>..HEAD -- src/services/builder_service.py` prints nothing (the data-engineer-owned file is untouched)
  </acceptance_criteria>
  <done>Missing data shows up as a named unverified item, the API returns one of exactly three verdicts, and the sidebar shows it with the messages escaped.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Name the wattage estimate, and style the new states</name>
  <files>src/services/compatibility_engine.py, src/static/app.js, src/static/style.css, tests/test_compatibility_rules.py</files>
  <read_first>
    - src/services/compatibility_engine.py (the file being modified; the wattage aggregate block after the RULES loop)
    - src/services/compatibility_rules.py (DEFAULT_CPU_TDP, DEFAULT_GPU_TDP)
    - src/static/app.js (the file being modified; the wattageEl line in validateBuild)
    - src/static/style.css (the file being modified; existing .compatibility-status.ok/.error and .warning-item.error/.warning rules, and the 860px media queries)
    - .planning/phases/01-launch-hardening/01-RESEARCH.md (Pattern 6, Pitfall 7: a separate code path from RULES)
  </read_first>
  <behavior>
    - CPU with tdp None named "Mystery CPU" gives an "estimate" item containing "Mystery CPU", "120" and "estimate", and it appears in BuildSummary.wattage_notes
    - The same for a GPU with 250
    - Both TDPs known gives no estimate item
    - No CPU or GPU gives no estimate item
    - estimate items do not change verdict or unverified_count
    - estimated_wattage values are unchanged from today for the same inputs
  </behavior>
  <action>
    Tests first. Per FIT-03, in validate_build's wattage block (Pitfall 7: separate from RULES), keep the same sums and the same estimated_wattage value.

    While summing, for each CPU view whose tdp is None, append CompatibilityWarning(level="estimate", message=f"Wattage estimate: {product_name} has no listed TDP, so a typical {DEFAULT_CPU_TDP} W was used."). Do the same for GPUs with DEFAULT_GPU_TDP. Place these after the PSU capacity warnings so the list order stays deterministic.

    Estimate items are informational. They are excluded from the verdict and from unverified_count (Task 1's computed fields only look at error, warning and unverified). This is Claude's discretion, and the SUMMARY records it for the owner.

    In app.js:
    - When summary.wattage_notes is non-empty, the wattage text reads "{n} W (estimate)" instead of "{n} W".
    - Render estimate items through the same escaped warnings list with class warning-item estimate.

    In style.css add:
    - .compatibility-status.unverified: an amber treatment, visually distinct from the green ok and red error.
    - .warning-item.unverified.
    - .warning-item.estimate: muted, but readable at normal size.

    Unverified items must be as visible as warnings: not collapsed, not hidden, not smaller text.
  </action>
  <verify>
    <automated>python -m pytest -q tests/test_compatibility_rules.py -k estimate</automated>
    <fails_when>non-zero exit, or the summary line shows fewer than 3 passed (for example "no tests ran" or "3 deselected")</fails_when>
  </verify>
  <acceptance_criteria>
    - `python -m pytest -q tests/test_compatibility_rules.py -k estimate` exits 0 with at least 3 passed
    - `git grep -n "compatibility-status.unverified" -- src/static/style.css` prints at least one line
    - `git grep -n "(estimate)" -- src/static/app.js` prints at least one line
    - `python -m pytest -q tests --ignore=tests/test_frontend_e2e.py` exits 0
  </acceptance_criteria>
  <done>Any wattage built on a default TDP names the part and says it is an estimate, in both the API response and the page.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The e2e suite can't pass by skipping; 375px flow; unverified verdict in the browser</name>
  <files>tests/test_frontend_e2e.py, src/static/style.css</files>
  <read_first>
    - tests/test_frontend_e2e.py (the file being modified; module-level importorskip, the base_url/browser/page fixtures, _open_builder, TestBuilderValidation.test_empty_build_reports_compatible, which asserts "Verified")
    - src/static/index.html (selectors: #search-input, #slots-container, #select-modal, #compatibility-status, #warnings-list, #compare-modal)
    - src/static/app.js (how catalog cards open compare with openCompareModal, and the top-level state object that holds builderSelections)
    - src/api/routes/builder.py (POST /candidates and POST /validate request shapes, to pick real ids read-only)
    - src/static/style.css (the file being modified if the 375px run finds overflow)
  </read_first>
  <behavior>
    - With REQUIRE_E2E=1: the browser fixture, the base_url fixture and the playwright import call pytest.fail instead of pytest.skip / importorskip
    - test_empty_build_reports_compatible asserts "All checks passed"
    - New test_gpu_with_case_missing_clearance_is_unverified: #compatibility-status reads "No problems found - 1 check unverified" and #warnings-list contains the case's name and "GPU clearance"
    - New TestMobile375 class with a mobile_page fixture (375x812):
      - search the catalog via #search-input and see result cards
      - open the PC Builder tab and click a Select in #slots-container
      - #select-modal becomes visible
      - choose the first option; the slot name updates and the modal closes
      - #compatibility-status is non-empty and equal to one of the three verdict strings
      - open compare from a catalog card; #compare-modal becomes visible
      - document.documentElement.scrollWidth is at most 376 on the catalog and builder views
  </behavior>
  <action>
    Per WEB-03: read os.environ.get("REQUIRE_E2E") == "1" once at module top. Replace the module-level pytest.importorskip for playwright with a try/except ImportError: under REQUIRE_E2E call pytest.fail(..., pytrace=False) at module level, otherwise pytest.skip(allow_module_level=True). In the base_url fixture's "app did not start" branch and in the browser fixture's except branch, call pytest.fail with the same reason when REQUIRE_E2E is set, and keep the skip otherwise. Dev machines without chromium still skip, but the deploy check (Phase 3's deploy script sets REQUIRE_E2E=1) and this phase's gate fail.

    Update test_empty_build_reports_compatible to assert "All checks passed" in the status text. Update its docstring to say the old "Verified" wording was replaced by the FIT-02 verdicts.

    Unverified e2e (FIT-02):
    - Find ids read-only through the page's own API with page.evaluate fetches: POST /api/v1/builder/candidates for slot "gpu" and slot "case".
    - Call POST /api/v1/builder/validate on GPU+case pairs until one returns unverified_count == 1 with a warning containing "GPU clearance". Cap at 40 validate calls, and fail with a clear message if none is found.
    - Then set state.builderSelections.gpu and .case in the page (page.evaluate can reach the top-level state binding) and call validateBuild().
    - Assert on the rendered DOM text.
    - Never call the save-build endpoint; tests must not write to the live DB.

    WEB-04: add a mobile_page fixture shaped like the existing page fixture, using browser.new_page(viewport={"width": 375, "height": 812}). Leave the existing page fixture unchanged. Drive the flow with the selectors listed in read_first. If the overflow assertion or a hidden or unclickable control fails, fix it in style.css inside a max-width media query. Do not change desktop layout.

    Finally run the full suite with REQUIRE_E2E=1. It includes the CSP from 01-01 and the pages from 01-02, so this is the first full browser gate of the phase.
  </action>
  <verify>
    <automated>REQUIRE_E2E=1 python -m pytest -q tests/test_frontend_e2e.py -k "Mobile or unverified or empty_build"</automated>
    <fails_when>non-zero exit, any "skipped" in the summary line, or fewer than 3 passed</fails_when>
    <automated>REQUIRE_E2E=1 python -m pytest -q tests</automated>
    <fails_when>non-zero exit, or the summary line contains "skipped" or "error"</fails_when>
  </verify>
  <acceptance_criteria>
    - `REQUIRE_E2E=1 python -m pytest -q tests` exits 0 and the summary line contains no "skipped"
    - `git grep -n "REQUIRE_E2E" -- tests/test_frontend_e2e.py` prints at least 3 lines
    - `git grep -n "width\": 375" -- tests/test_frontend_e2e.py` prints at least one line
    - `git grep -n "All checks passed" -- tests/test_frontend_e2e.py` prints at least one line
    - The SUMMARY lists the GPU and case product ids the unverified e2e found in the live catalog
  </acceptance_criteria>
  <done>The browser suite fails instead of silently skipping when the deploy check requires it, the phone-width flow works end to end, and the unverified verdict is proven in a real page.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| retailer titles (scraped, untrusted) -> warning messages -> innerHTML | Product names flow from third-party pages into the DOM |
| engine verdict -> shopper decision | A wrong "passed" leads to a physical purchase that doesn't fit |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-09 | Tampering (stored XSS) | app.js warnings rendering | high | mitigate | Every warning message passes through escapeHtml before innerHTML; grep gate on escapeHtml(w.message) (Task 1) |
| T-1-10 | Repudiation / integrity of trust | verdict for missing data | high | mitigate | unverified level plus the 3-state verdict; "All checks passed" impossible while unverified_count > 0; unit plus e2e tests (Tasks 1, 3) |
| T-1-11 | Denial of Service (quality gate) | e2e suite silently skipped | medium | mitigate | REQUIRE_E2E=1 turns every skip path into a failure (Task 3) |
| T-1-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no packages |
</threat_model>

<verification>
- `python -m pytest -q tests --ignore=tests/test_frontend_e2e.py` exits 0
- `REQUIRE_E2E=1 python -m pytest -q tests` exits 0 with no skips
- Manual (VALIDATION.md manual row): open the builder, pick a GPU and a case with no clearance, and confirm the sidebar reads "No problems found - 1 check unverified" and names the case and "GPU clearance"
</verification>

<success_criteria>
ROADMAP Phase 1 Success Criteria 1 and 6 (the full suite, including every Playwright test, passes). FIT-01, FIT-02, FIT-03, WEB-03 and WEB-04 are green.
</success_criteria>

## Artifacts this phase produces (this plan's share)

| Symbol / file | Kind | Consumers |
|---|---|---|
| `services.compatibility_rules.FIELD_LABELS` | dict | engine messages; FIT-04 margins (Phase 4) |
| `services.compatibility_rules.rule_applies(rule, view_a, view_b)` | function | validate_build |
| CompatibilityWarning levels "unverified" and "estimate" | schema values | app.js, Phase 4/5 UI |
| `BuildSummary.unverified_count`, `.verdict`, `.wattage_notes` | computed fields | /builder/validate response, app.js |
| view attribute `product_name` on merged spec views | engine data | messages |
| `.compatibility-status.unverified`, `.warning-item.unverified`, `.warning-item.estimate` | CSS | sidebar |
| `REQUIRE_E2E` env switch, `mobile_page` fixture, `TestMobile375` | test infra | WEB-03 deploy check (Phase 3 deploy.sh) |

## Flagged assumptions (edge probe: unclassified rows, never silently dropped)

- **WEB-04 (unclassified):** "Can open compare" is taken to mean opening compare from a catalog card, the only compare entry point today. "Usable" is measured as: no horizontal overflow, and every step's control visible and clickable at 375px.
- **FIT-02 wording choice (Claude's discretion, flag to owner):** fired warning-level items (PSU headroom, cooler socket, radiator) count as "Problems found", because showing "No problems found" next to a visible warning would contradict itself. Estimate notes don't change the verdict. An empty build shows "All checks passed".

<output>
Create `.planning/phases/01-launch-hardening/01-03-SUMMARY.md` when done.
</output>
