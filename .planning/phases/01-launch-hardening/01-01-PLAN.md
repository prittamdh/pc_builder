---
phase: 01-launch-hardening
plan: 01
type: execute
wave: 1
depends_on: []
owner: web-dev
files_modified:
  - src/configs/settings.py
  - src/api/main.py
  - .env.example
  - tests/test_cors.py
  - tests/test_docs_disabled.py
  - tests/test_settings.py
  - tests/test_security_headers.py
  - tests/test_env_example.py
autonomous: true
requirements: [SEC-01, SEC-02, SEC-05, SEC-06, SEC-08]
user_setup: []

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A request to the API with Origin https://evil.example gets no Access-Control-Allow-Origin header, and the response never carries Access-Control-Allow-Credentials: true (SEC-01)"
    - "An Origin listed in CORS_ALLOWED_ORIGINS gets Access-Control-Allow-Origin echoing exactly that origin (SEC-01)"
    - "Starting the API with DATABASE_URL unset (and no .env loaded) exits non-zero with an error naming DATABASE_URL; no database credential literal exists in src/, dags/, scripts/ or docker-compose.yml (SEC-02)"
    - "GET / carries X-Content-Type-Options: nosniff, a Referrer-Policy header, and a Content-Security-Policy that contains frame-ancestors 'none' (SEC-05)"
    - "The app itself never sends Strict-Transport-Security; HSTS is the Phase 3 proxy's job (SEC-05, carried to 03-02)"
    - "With ENV=production, GET /docs, /redoc and /openapi.json all return 404; with ENV unset they return 200 (SEC-06)"
    - "No path under /api/agent appears in the dev-mode OpenAPI schema (SEC-06 forward guard for Phase 2)"
    - "tests/test_env_example.py fails if any variable read in src/configs/settings.py, read via os.getenv/os.environ anywhere in src/, or referenced as ${VAR} in docker-compose*.yml is missing from .env.example (SEC-08)"
    - "[EDGE SEC-08/adjacency] A variable used by both settings.py and docker-compose.yml appears exactly once in .env.example; a duplicated key fails the test"
    - "[EDGE SEC-08/empty] Every value in .env.example is empty or an angle-bracket placeholder like <your-key>; any other value fails the test"
    - "[EDGE SEC-08/ordering] The .env.example check compares key sets, so it passes or fails the same way whatever order the keys are written in"
    - statement: "[EDGE SEC-08/concurrency] tests/test_env_example.py only reads files, so running it alongside other tests changes nothing"
      verification: backstop
    - statement: "[EDGE SEC-01/concurrency] The CORS allow-list is fixed once at app construction; concurrent requests from listed and unlisted origins each get the correct header because no per-request mutable state exists"
      verification: backstop
    - statement: "[EDGE SEC-06/concurrency] The docs on/off decision is read once when the app is built; a running process never flips between exposing and hiding /docs"
      verification: backstop
  prohibitions:
    - requirement_id: SEC-08
      category: safety
      status: resolved
      verification: test
      flagged: true
      statement: "The .env.example file and every test fixture must never contain a real secret, API key or password, including values copied from the developer's own .env"
    - requirement_id: SEC-02
      category: safety
      status: resolved
      verification: judgment
      flagged: true
      statement: "The refuse-to-start error must name the missing variable only; it must never print any part of a connection string or other environment values"
  artifacts:
    - path: src/configs/settings.py
      provides: "ENV, CORS_ALLOWED_ORIGINS, TRUST_CF_CONNECTING_IP, RATE_LIMIT_* strings, CONTACT_EMAIL, require_database_url()"
      contains: "def require_database_url"
    - path: src/api/main.py
      provides: "explicit-origin CORS, env-gated docs, security-headers middleware, apply_security_headers() helper"
      contains: "frame-ancestors 'none'"
    - path: .env.example
      provides: "every variable the code and compose files read, with no real values"
    - path: tests/test_env_example.py
      provides: "settings/compose vs .env.example cross-check"
  key_links:
    - from: src/api/main.py
      to: src/configs/settings.py
      via: "settings.require_database_url() called before api.routes is imported; settings.ENV and settings.CORS_ALLOWED_ORIGINS read at app construction"
      pattern: "require_database_url\\(\\)"
    - from: tests/test_env_example.py
      to: .env.example
      via: "parses KEY= lines and compares to os.getenv keys and ${VAR} references"
      pattern: "env.example"
---

<objective>
Close the configuration-level security holes before anything is public: CORS stops trusting every origin, the app refuses to start without an explicit database URL (and the default credential leaves the source), every response carries the SEC-05 security headers, API docs are hidden in production, and `.env.example` documents every variable, checked by a test.

Purpose: Phase goal "nothing embarrassing or unsafe goes public" (ROADMAP Phase 1, Success Criterion 2). This plan also owns the phase's whole config inventory (rate-limit strings for 01-05, the contact address for 01-02) so later plans don't each edit settings.py and `.env.example`.
Output: hardened `src/api/main.py`, extended `src/configs/settings.py`, new `.env.example`, five new test files.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-launch-hardening/01-RESEARCH.md
@.planning/phases/01-launch-hardening/01-PATTERNS.md
@.claude/agents/web-dev.md
@src/api/main.py
@src/configs/settings.py
@docker-compose.yml

Locked decisions that apply here (ROADMAP "Decisions Needed from Owner" plus RESEARCH "Locked Decisions"; no CONTEXT.md exists):
- Hosting is Oracle Always Free behind Cloudflare. No Redis or other extra infrastructure.
- API docs are off in production (owner, "everything else as recommended").
- HSTS belongs to the production proxy (Phase 3), not the app.
- The CSP keeps 'unsafe-inline' for scripts and styles this phase: index.html and app.js use inline onclick= and style= attributes that a strict policy would silently break. Tightening is a later item (see Flagged assumptions).
</context>

<assumption_delta_decision>
The detector fired on "a request from another site's origin" (pluralization cue "another"). This is a false positive: no second platform, tenant or identity is introduced. The CORS allow-list stays a flat list of origins. Decision: no-change. Primary noun unchanged.
</assumption_delta_decision>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): ENV flag and explicit CORS allow-list, end to end from settings to the HTTP response</name>
  <files>src/configs/settings.py, src/api/main.py, tests/test_cors.py, tests/test_docs_disabled.py</files>
  <read_first>
    - src/configs/settings.py (the file being modified; keep its module-level os.getenv style, no class, no pydantic BaseSettings)
    - src/api/main.py (the file being modified; current CORS block and FastAPI(...) constructor)
    - .planning/phases/01-launch-hardening/01-RESEARCH.md (Pattern 1, Code Example "CORS with an explicit allow-list")
    - .planning/phases/01-launch-hardening/01-PATTERNS.md (sections for src/api/main.py and src/configs/settings.py, and "No Analog Found" for test_docs_disabled.py)
    - tests/test_image_proxy.py (plain-assert test style to mirror)
  </read_first>
  <behavior>
    - test_cors: Origin https://evil.example on GET /health returns no access-control-allow-origin header
    - test_cors: an OPTIONS preflight from https://evil.example to /api/v1/builder/validate gets no access-control-allow-origin header
    - test_cors: with CORS_ALLOWED_ORIGINS containing https://allowed.example, that origin is echoed back exactly
    - test_cors: no response ever has access-control-allow-credentials equal to "true"
    - test_docs_disabled: ENV=production makes /docs, /redoc and /openapi.json return 404
    - test_docs_disabled: ENV unset makes all three return 200
    - test_docs_disabled: in dev mode no key of the OpenAPI "paths" object starts with /api/agent
  </behavior>
  <action>
    Write the two test files first and run them to see them fail (project rule: test first).

    In src/configs/settings.py add, in a new "# --- Runtime environment / web security ---" banner section using the file's existing os.getenv style: ENV (default "development"); CORS_ALLOWED_ORIGINS, parsed from a comma-separated env string into a list of stripped, non-empty origins (default empty list, meaning no cross-origin access at all).

    In src/api/main.py (per SEC-01 and SEC-06): import settings via "from configs import settings". Compute a docs flag as settings.ENV != "production" and pass docs_url, redoc_url and openapi_url to FastAPI(...) as their usual paths when the flag is on and None when off. Replace the CORS middleware arguments with allow_origins=settings.CORS_ALLOWED_ORIGINS, allow_credentials=False, allow_methods=["GET", "POST"], allow_headers=["Content-Type"]. The app has no cookies or sessions, so credentials are never needed.

    Tests use fastapi.testclient.TestClient against api.main.app. Because ENV and CORS_ALLOWED_ORIGINS are read at import time, the tests set the env var with monkeypatch.setenv, call importlib.reload on configs.settings and then on api.main, and reload again with the env removed in teardown so later tests see the dev app. Only request routes that don't touch the database (/health, /docs, /openapi.json, OPTIONS preflights); tests must not write to the live DB.
  </action>
  <verify>
    <automated>python -m pytest -q tests/test_cors.py tests/test_docs_disabled.py</automated>
    <fails_when>non-zero exit, or the summary line reports fewer than 7 passed</fails_when>
  </verify>
  <acceptance_criteria>
    - `python -m pytest -q tests/test_cors.py tests/test_docs_disabled.py` exits 0 with at least 7 tests passed
    - `git grep -n "allow_credentials=True" -- src/api/main.py` prints nothing
    - `git grep -n "allow_credentials=False" -- src/api/main.py` prints one line
    - `git grep -n "openapi_url" -- src/api/main.py` prints at least one line
    - `git grep -n "CORS_ALLOWED_ORIGINS" -- src/configs/settings.py` prints at least one line
  </acceptance_criteria>
  <done>An unlisted origin gets no CORS permission, a listed one does, and production mode serves no API docs or schema.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refuse to start without DATABASE_URL, and add the SEC-05 security headers</name>
  <files>src/configs/settings.py, src/api/main.py, tests/test_settings.py, tests/test_security_headers.py</files>
  <read_first>
    - src/configs/settings.py (the file being modified; the DATABASE_URL block at the bottom)
    - src/api/main.py (the file being modified, as left by Task 1)
    - src/db/connection.py (read only: it does a substring check on the URL at import, so a None URL raises TypeError there; this is why the guard must run first)
    - .planning/phases/01-launch-hardening/01-RESEARCH.md (Pattern 2, Pitfall 1 on CSP vs inline handlers, Pitfall 2 on HSTS)
    - src/static/style.css and src/static/index.css (line 1 of each imports Google Fonts, which the CSP must allow)
  </read_first>
  <behavior>
    - test_settings: require_database_url() raises RuntimeError whose message contains "DATABASE_URL" when the value is None or an empty string
    - test_settings: a subprocess that turns dotenv loading into a no-op, removes DATABASE_URL from its environment and imports api.main exits non-zero with "DATABASE_URL" in stderr
    - test_settings: the refuse-to-start stderr contains no "postgresql" substring
    - test_security_headers: GET / has x-content-type-options == "nosniff"
    - test_security_headers: GET / has referrer-policy == "strict-origin-when-cross-origin"
    - test_security_headers: GET / has a content-security-policy containing "frame-ancestors 'none'" plus the fonts.googleapis.com and fonts.gstatic.com allowances
    - test_security_headers: GET / has no strict-transport-security header
    - test_security_headers: a JSON response (GET /health) carries the same three headers
  </behavior>
  <action>
    Tests first, run and see them fail.

    SEC-02, in src/configs/settings.py: change DATABASE_URL to os.getenv("DATABASE_URL") with no default, so the default credential string is deleted from the file entirely. Add require_database_url() -> str: it returns the value, and raises RuntimeError("DATABASE_URL is not set. Copy .env.example to .env and set it.") when the value is None or blank. The message must never include any URL or other environment value. Do not raise at settings import time: scripts such as the Phase 1 benchmark import settings on machines with no database.

    In src/api/main.py call settings.require_database_url() at module level BEFORE the line that imports api.routes. The routes import db.connection, which would otherwise fail first with an unhelpful TypeError. This is what makes uvicorn refuse to start.

    SEC-05, in src/api/main.py: add a module-level function apply_security_headers(response) that sets these headers:
    - X-Content-Type-Options: nosniff
    - Referrer-Policy: strict-origin-when-cross-origin
    - Content-Security-Policy with these directives, joined by "; ":
      - default-src 'self'
      - script-src 'self' 'unsafe-inline'
      - style-src 'self' 'unsafe-inline' https://fonts.googleapis.com
      - font-src 'self' https://fonts.gstatic.com
      - img-src 'self' data:
      - connect-src 'self'
      - frame-ancestors 'none'
      - base-uri 'self'
      - object-src 'none'

    Register an @app.middleware("http") function that calls it on every response. Plan 01-02 reuses apply_security_headers on its 500 handler, because Starlette's server-error layer sits outside user middleware.

    Do not add the HSTS header anywhere in the app (Pitfall 2: it poisons local dev browsers; Phase 3's Caddy adds it). Keep 'unsafe-inline' (Pitfall 1). Add a comment above the CSP naming the inline onclick/style attributes as the reason, and saying tightening is a later item.

    test_settings.py subprocess recipe:
    - Run sys.executable -c with a snippet that sets dotenv.load_dotenv to a no-op lambda, puts src on sys.path, then imports api.main.
    - Pass env as a copy of os.environ without DATABASE_URL, with cwd at the repo root.
    - Assert returncode != 0 and "DATABASE_URL" in stderr.
  </action>
  <verify>
    <automated>python -m pytest -q tests/test_settings.py tests/test_security_headers.py</automated>
    <fails_when>non-zero exit, or the summary line reports fewer than 7 passed</fails_when>
    <automated>git grep -n "pc_builder123" -- src dags scripts docker-compose.yml</automated>
    <fails_when>any matching line is printed (git grep exits 0); a clean tree prints nothing and exits 1</fails_when>
  </verify>
  <acceptance_criteria>
    - `python -m pytest -q tests/test_settings.py tests/test_security_headers.py` exits 0
    - `git grep -n "pc_builder123" -- src dags scripts docker-compose.yml` prints nothing
    - `git grep -n "def require_database_url" -- src/configs/settings.py` prints one line
    - `git grep -n "frame-ancestors 'none'" -- src/api/main.py` prints one line
    - `git grep -in "strict-transport-security" -- src/api/main.py` prints nothing
    - `python -m pytest -q tests --ignore=tests/test_frontend_e2e.py` exits 0 (no regression; the local .env still supplies DATABASE_URL)
  </acceptance_criteria>
  <done>The API won't start without an explicit database URL, the default credential is gone from source, and every response carries nosniff, Referrer-Policy and a CSP with frame-ancestors 'none'.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Phase 1 config inventory, .env.example, and the cross-check test</name>
  <files>src/configs/settings.py, .env.example, tests/test_env_example.py</files>
  <read_first>
    - src/configs/settings.py (the file being modified; every os.getenv key)
    - docker-compose.yml (every ${VAR} reference: POSTGRES_PASSWORD, PGADMIN_DEFAULT_EMAIL, PGADMIN_DEFAULT_PASSWORD, AIRFLOW_FERNET_KEY, AIRFLOW_WEBSERVER_SECRET_KEY)
    - .gitignore (confirm .env is ignored and .env.example is not)
    - .planning/phases/01-launch-hardening/01-PATTERNS.md (section ".env.example")
    - .planning/phases/01-launch-hardening/01-RESEARCH.md (Pattern 3: rate-limit key and the CF-Connecting-IP trust boundary; Open Question 3: contact address)
  </read_first>
  <behavior>
    - Every key read by os.getenv or os.environ in src/configs/settings.py is in .env.example
    - Every key read by os.getenv or os.environ under src/ is in .env.example
    - Every ${VAR} in docker-compose*.yml is in .env.example
    - No key appears twice in .env.example
    - Every value is empty or matches ^<[^>]+>$
    - The file contains no "pc_builder123", no "sk-", no "gsk_" and no "AIza"
  </behavior>
  <action>
    Test first.

    Add the remaining Phase 1 settings to src/configs/settings.py, each in the existing os.getenv style, so plans 01-02 and 01-05 only read them:
    - TRUST_CF_CONNECTING_IP: bool parsed from "true"/"false", default false. Only when true may the 01-05 rate-limit key read the CF-Connecting-IP header. Phase 3 turns it on after OPS-02 firewalls the origin to Cloudflare ranges. Until then the header is attacker-controlled, so the key uses the socket address.
    - RATE_LIMIT_ENABLED: bool, default true.
    - RATE_LIMIT_DEFAULT: default "300/minute".
    - RATE_LIMIT_IMAGES: default "120/minute".
    - RATE_LIMIT_BUILDER: default "60/minute".
    - RATE_LIMIT_SAVE_BUILD: default "10/minute".
    - CONTACT_EMAIL: default empty string. Per WEB-05, the owner has not chosen an address, and an empty value is the clearly marked placeholder. Never put an invented address here.

    Create .env.example at the repo root:
    - One KEY= line per variable, grouped under "# ---" banner comments that match settings.py.
    - A one-line comment above each key saying what it is.
    - Values empty or angle-bracket placeholders only, e.g. DATABASE_URL=<postgresql+psycopg://user:password@host:5432/pc_builder>.
    - It must cover every settings.py variable (REQUEST_TIMEOUT, MAX_RETRIES, BACKOFF_FACTOR, LOG_LEVEL, GROQ_API_KEY, CEREBRAS_API_KEY, MISTRAL_API_KEY, GOOGLE_API_KEY, NVIDIA_API_KEY, FUZZY_MATCH_THRESHOLD, DATABASE_URL, ENV, CORS_ALLOWED_ORIGINS, TRUST_CF_CONNECTING_IP, the four RATE_LIMIT_ strings plus RATE_LIMIT_ENABLED, CONTACT_EMAIL) and the five docker-compose variables.
    - A comment near the rate-limit keys saying the API must run as a single process, because limiter counters are in memory and each extra worker multiplies the effective limit.
    - A comment near CONTACT_EMAIL: "leave empty until the owner picks the address; the About page then shows a 'contact address coming soon' note".
    - A comment near TRUST_CF_CONNECTING_IP: "set true only once the origin accepts traffic from Cloudflare IPs only (Phase 3, OPS-02)".
    - Do not read or copy the real .env (a hook blocks it, and SEC-08 forbids real values).

    tests/test_env_example.py:
    - Collect code keys with a regex over os.getenv("KEY" and os.environ["KEY" / os.environ.get("KEY" across src/**/*.py.
    - Collect compose keys with a regex over ${KEY} and ${KEY:-...} across docker-compose*.yml at the repo root.
    - Parse .env.example into (key, value) pairs, skipping blank and comment lines.
    - Assert: the code and compose key sets are subsets of the example keys; there are no duplicate keys; every value is empty or an angle-bracket placeholder; and the forbidden substrings from the behavior list are absent.
    - Use set comparisons, so key order never matters.
    - The SEC-08 clause about .env being mode 600 and owned by the service user on the VM cannot be tested here (no VM until Phase 3). Record it in the SUMMARY as carried to Phase 3's docs/DEPLOY.md.
  </action>
  <verify>
    <automated>python -m pytest -q tests/test_env_example.py</automated>
    <fails_when>non-zero exit, or the summary line reports 0 passed</fails_when>
  </verify>
  <acceptance_criteria>
    - `python -m pytest -q tests/test_env_example.py` exits 0
    - `git grep -c "^CONTACT_EMAIL=$" -- .env.example` prints 1 (the value is empty, not a made-up address)
    - `git grep -n "RATE_LIMIT_IMAGES" -- src/configs/settings.py .env.example` prints a line from each file
    - `git grep -n "TRUST_CF_CONNECTING_IP" -- src/configs/settings.py .env.example` prints a line from each file
    - `git check-ignore .env.example` prints nothing (the file is tracked, not ignored)
    - `python -m pytest -q tests --ignore=tests/test_frontend_e2e.py` exits 0
    - `python -m pytest -q tests/test_frontend_e2e.py -k TestPageLoads` exits 0 with 3 passed and 0 skipped, so test_no_console_errors_on_load passes under the new CSP. The full browser suite runs in plan 01-03.
  </acceptance_criteria>
  <done>Every variable the code and compose files read is in .env.example with no real values, a test keeps it that way, and the Phase 1 config for rate limits, proxy trust and the contact address is in one place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (any origin) -> API | Cross-origin pages can script requests to the API; CORS decides whether they may read responses |
| repo -> public | Anything committed is effectively public once the repo or a leak exposes it |
| API -> browser rendering | Response headers control framing, MIME sniffing and script sources |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-01 | Information Disclosure | CORS middleware in src/api/main.py | high | mitigate | Explicit allow-list from CORS_ALLOWED_ORIGINS (empty by default), allow_credentials=False; tests/test_cors.py asserts no ACAO for unlisted origins (Task 1) |
| T-1-02 | Information Disclosure | DATABASE_URL default in src/configs/settings.py | high | mitigate | Default credential removed; require_database_url() fails closed before routes import; git grep gate on the literal (Task 2) |
| T-1-03 | Tampering | Clickjacking / MIME sniffing on HTML responses | medium | mitigate | apply_security_headers(): frame-ancestors 'none', nosniff, Referrer-Policy on every response (Task 2) |
| T-1-04 | Information Disclosure | /docs, /redoc, /openapi.json in production | medium | mitigate | openapi_url/docs_url/redoc_url None when ENV=production; tests/test_docs_disabled.py (Task 1) |
| T-1-05 | Elevation of Privilege | CSP allows 'unsafe-inline' scripts | medium | accept | Required by 16+ inline onclick/style attributes this phase (RESEARCH Pitfall 1). Accepted until handlers move to addEventListener; the XSS sink that matters (warning messages rendered via innerHTML) is escaped in plan 01-03 |
| T-1-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no packages; the slowapi install is gated in plan 01-05 |
</threat_model>

<verification>
- `python -m pytest -q tests --ignore=tests/test_frontend_e2e.py` exits 0
- `python -m pytest -q tests/test_frontend_e2e.py -k TestPageLoads` exits 0 (the CSP doesn't produce console errors on load)
- `git grep -n "pc_builder123" -- src dags scripts docker-compose.yml` prints nothing
</verification>

<success_criteria>
ROADMAP Phase 1 Success Criterion 2, partly: another site's origin gets no CORS permission, and /docs is gone in production mode. The 429 half is delivered by plan 01-05. SEC-01, SEC-02, SEC-05 (app-side headers), SEC-06 and SEC-08 (repo-side) are green by test.
</success_criteria>

## Artifacts this phase produces (this plan's share)

| Symbol / file | Kind | Consumers |
|---|---|---|
| `configs.settings.ENV` | str setting | api.main (docs toggle); Phase 3 compose |
| `configs.settings.CORS_ALLOWED_ORIGINS` | list[str] setting | api.main CORS |
| `configs.settings.TRUST_CF_CONNECTING_IP` | bool setting | 01-05 rate-limit key function |
| `configs.settings.RATE_LIMIT_ENABLED`, `RATE_LIMIT_DEFAULT`, `RATE_LIMIT_IMAGES`, `RATE_LIMIT_BUILDER`, `RATE_LIMIT_SAVE_BUILD` | settings | 01-05 limiter |
| `configs.settings.CONTACT_EMAIL` | str setting (empty = placeholder) | 01-02 /about route |
| `configs.settings.require_database_url()` | function | api.main startup guard |
| `api.main.apply_security_headers(response)` | function | security middleware; 01-02 500 handler |
| `.env.example` | file | owner, Phase 3 deploy |
| `tests/test_cors.py`, `tests/test_docs_disabled.py`, `tests/test_settings.py`, `tests/test_security_headers.py`, `tests/test_env_example.py` | tests | phase gate |

## Flagged assumptions (edge probe: unclassified rows, never silently dropped)

- **SEC-02 (unclassified):** Assumed that an empty or whitespace-only DATABASE_URL counts as unset and also refuses to start. Covered by a Task 2 test. The owner's `.env` must set it, and Airflow's compose block already sets it explicitly.
- **SEC-05 (unclassified):** The HSTS clause and the "curl check on production" can't be met in Phase 1 (no production host). Carried to plan 03-02's Caddyfile. The CSP keeps 'unsafe-inline'. Tightening it (moving inline handlers to addEventListener) is a later item, not in this phase.
- **SEC-08 (VM clause):** `.env` mode 600, owned by the service user, with LLM keys only there: no VM exists until Phase 3. Carried to `docs/DEPLOY.md` in 03-02.

<output>
Create `.planning/phases/01-launch-hardening/01-01-SUMMARY.md` when done.
</output>
