---
phase: 01-launch-hardening
plan: 06
type: execute
wave: 1
depends_on: []
owner: data-engineer
files_modified:
  - scripts/benchmark_provider.py
  - tests/test_benchmark_provider.py
autonomous: true
requirements: [AI-01]
user_setup: []

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "scripts/benchmark_provider.py is committed with a fixed PSU case list (title, expected 80 PLUS tier or null, and the evidence for each answer) and a scoring function (AI-01)"
    - "Every locked case's expected answer is backed by the 80 PLUS registry export (data/raw/All_certified_psus.xlsx) or the manufacturer's own page, recorded in the case's evidence field; no answer comes from this project's own LLM extractions"
    - "The case count N is 8 if 8 answers can be independently verified, otherwise the true smaller number, stated in the script's docstring and the SUMMARY; no unverified case is included to reach 8"
    - "It runs against any (name, url, model, key) provider: --provider NAME picks from provider_chain(), and --url/--model/--key-env run an arbitrary OpenAI-compatible endpoint (for the Phase 8 local trial)"
    - "It prints 'Score: k/N', titles/min and the JSON-failure count, and exits non-zero if the provider can't be reached at all"
    - "It never substitutes another provider: the service's fallback list is emptied, and the printed provider name equals the one requested"
    - "It writes nothing to the database: it never imports db.session and never uses the DB-hint prompt builder (only the bare PSU_IDENTITY_BATCH_PROMPT)"
    - "Live runs against mistral and google are recorded in the SUMMARY with their real scores (ROADMAP Success Criterion 5 expects N/N for both)"
    - "[EDGE AI-01/boundary] All N correct prints Score: N/N; one wrong prints Score: (N-1)/N; the score is an integer count over N"
    - "[EDGE AI-01/empty] A missing, malformed or empty result for a title counts as wrong and adds to JSON failures; the run completes and still prints all three lines"
    - "[EDGE AI-01/encoding] Ratings are compared after normalizing: case-insensitive, whitespace collapsed, and '80 PLUS', '80+', '80Plus' and '80 Plus' treated as equal; null, empty string and the string 'null' all mean no tier"
    - "[EDGE AI-01/precision] titles/min = titles sent / elapsed wall seconds * 60, printed to one decimal place; elapsed time covers only the provider calls"
  prohibitions:
    - requirement_id: AI-01
      category: values
      status: resolved
      verification: judgment
      flagged: true
      statement: "The answer key must never be derived from, or tuned to match, any model's output, including this project's stored extractions or the live benchmark runs; a case a provider gets wrong stays wrong"
    - requirement_id: AI-01
      category: safety
      status: resolved
      verification: test
      flagged: true
      statement: "API keys must never be accepted as command-line values or printed; the script reads keys only from environment variables named by --key-env or from settings"
  artifacts:
    - path: scripts/benchmark_provider.py
      provides: "CASES, normalize_rating, score_results, run_benchmark, main"
      contains: "PSU_IDENTITY_BATCH_PROMPT"
    - path: tests/test_benchmark_provider.py
      provides: "offline scoring and no-DB tests using a fake service"
  key_links:
    - from: scripts/benchmark_provider.py
      to: src/services/groq_extraction_service.py
      via: "GroqExtractionService(api_key, model, api_url).extract_batch(PSU_IDENTITY_BATCH_PROMPT, titles)"
      pattern: "extract_batch\\("
---

<objective>
Commit the fixed PSU benchmark AI-01 describes, so any provider (and later the local RX 9060 XT model in Phase 8) can be scored the same way. RESEARCH found the original 8 titles and answers were never committed, so this plan rebuilds the benchmark from real catalog titles whose answers are independently verified. It does not recall or guess them.

Purpose: ROADMAP Phase 1 Success Criterion 5, and the input to Phase 8's gate (8/8 and at least 60 titles/min).
Output: scripts/benchmark_provider.py, tests/test_benchmark_provider.py, and live scores for mistral and google in the SUMMARY.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-launch-hardening/01-RESEARCH.md
@.planning/phases/01-launch-hardening/01-PATTERNS.md
@.claude/agents/data-engineer.md
@src/services/groq_extraction_service.py
@scripts/ask_free_ai.py

Project rule (owner): benchmark answers must be independently verified against the 80 PLUS registry or the manufacturer, never guessed. Fewer than 8 locked cases is acceptable if some can't be verified, as long as the number is stated honestly.
The data-engineer rule "LLM calls go through default_service()" has a deliberate exception here: a benchmark must bind to one named provider, and default_service() auto-picks. Constructing GroqExtractionService directly is the point.
Runs in parallel with 01-04 and web-dev's plans. No shared files.
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): Benchmark script end to end with one verified case and a fake provider</name>
  <files>scripts/benchmark_provider.py, tests/test_benchmark_provider.py</files>
  <read_first>
    - src/services/groq_extraction_service.py (provider_chain, GroqExtractionService.__init__ which auto-fills self._fallbacks, extract_batch's return shape [{"parsed", "raw_response", "model"}], the bisect-on-error and fallback-on-exhaustion branches, PSU_IDENTITY_BATCH_PROMPT and its few-shot titles)
    - scripts/ask_free_ai.py (sys.path/encoding setup, argparse main(argv) -> int, error handling)
    - scripts/import_80plus_efficiency.py (how the registry xlsx is loaded and matched; reuse its loader for verification)
    - .planning/phases/01-launch-hardening/01-RESEARCH.md (Pattern 8, "Reconstructing the AI-01 benchmark", Pitfall 9)
  </read_first>
  <behavior>
    - normalize_rating("80 PLUS Gold") == normalize_rating("80+ gold") == normalize_rating(" 80Plus  GOLD ") == "80+ gold"
    - normalize_rating(None) == normalize_rating("") == normalize_rating("null") == None
    - score_results(cases, results) with all correct returns (N, 0), one wrong returns (N-1, 0), and one index missing returns (N-1, 1)
    - run_benchmark(fake_service, cases) returns score, json_failures and titles_per_min, with titles_per_min > 0, and uses a monotonic clock
    - main(["--provider", "nosuch"]) returns non-zero with a message naming the available providers
    - A subprocess that imports benchmark_provider leaves "db.session" out of sys.modules
    - The script source contains no identity_prompt reference and no --key argument
  </behavior>
  <action>
    Tests first. The fake service is a stub object with an extract_batch(prompt, titles) method returning canned parsed dicts. No network.

    Create scripts/benchmark_provider.py (per AI-01):
    - A module docstring stating what it measures, that it writes nothing to the DB, how the cases were built (reconstructed 2026-09 from live catalog titles; the original 2026-09-20 set was never committed), and the final case count N.
    - Copy the sys.path setup and stdout utf-8 guard from ask_free_ai.py.
    - CASES: a list of dicts with keys title, expected (a normalized tier string or None), trap (one line on what the case tests) and evidence (source plus the exact registry row or URL, and the date checked).

    In this task lock exactly one case: product 16593, "Super Flower LEADEX III GOLD UP ATX 3.1 750W Cybenetics Platinum Certified Gold SMPS Power Supply", the Cybenetics trap quoted in PROGRESS.md. Verify it against the registry first using import_80plus_efficiency's loader: Super Flower Leadex III Gold UP 750W should appear at the Gold level. Record the matched registry row in evidence. If the registry disagrees, the registry wins, and you record that.

    Functions:
    - normalize_rating(value) -> str | None
    - score_results(cases, results) -> tuple[int, int]: correct count and JSON failures. It matches on the parsed "index" (1-based). A missing or unparseable entry counts as wrong and as one JSON failure.
    - run_benchmark(service, cases) -> dict: one extract_batch call with all titles, timed with time.monotonic.
    - main(argv=None) -> int, with argparse:
      - --provider NAME: looked up in provider_chain() by name. If unknown or its key is unset, exit 2 and list the names available.
      - OR --url, --model, and --key-env VAR, which reads the key from os.environ[VAR]. For keyless local servers, also accept --key-env-optional to send a dummy placeholder key.
      - --runs K, default 1.

    Build GroqExtractionService(api_key=..., model=..., api_url=...) and set its _fallbacks to an empty list right after construction, so it can never switch to another provider. Print "Provider: <name> (<model>)", then "Score: k/N", "Titles/min: x.x" and "JSON failures: j" per run.

    Catch GroqExtractionError and ProviderExhausted: print them to stderr and return 1. Never accept a key value as an argument, and never print a key.

    Use the bare PSU_IDENTITY_BATCH_PROMPT constant, imported from services.groq_extraction_service. Never use the variant that appends DB brand hints: it reads the DB and makes runs non-reproducible. Do not import the project's DB session module anywhere, directly or indirectly.
  </action>
  <verify>
    <automated>python -m pytest -q tests/test_benchmark_provider.py</automated>
    <fails_when>non-zero exit, or the summary line reports fewer than 7 passed</fails_when>
  </verify>
  <acceptance_criteria>
    - `python -m pytest -q tests/test_benchmark_provider.py` exits 0
    - `git grep -n "identity_prompt" -- scripts/benchmark_provider.py` prints nothing
    - `git grep -n -e "db.session" -e SessionLocal -- scripts/benchmark_provider.py` prints nothing
    - `git grep -n "_fallbacks" -- scripts/benchmark_provider.py` prints at least one line
    - `python scripts/benchmark_provider.py --provider nosuch` exits non-zero and lists available provider names
  </acceptance_criteria>
  <done>The benchmark runs end to end against a fake provider, scores exactly, cannot touch the DB or swap providers, and holds one registry-verified case.</done>
</task>

<task type="auto">
  <name>Task 2: Independently verify the remaining answers and lock the case list</name>
  <files>scripts/benchmark_provider.py, tests/test_benchmark_provider.py</files>
  <read_first>
    - scripts/benchmark_provider.py (the file being modified; CASES)
    - scripts/import_80plus_efficiency.py (registry loader and matching rules)
    - .planning/phases/01-launch-hardening/01-RESEARCH.md (candidate table: ids 3960, 20457/17180, 16759/18924/14917, 6186, 16673/19204, 15310, plus the recommended untiered case)
    - src/services/groq_extraction_service.py (PSU_IDENTITY_BATCH_PROMPT few-shot titles, which must not be reused as cases)
  </read_first>
  <action>
    For each RESEARCH candidate, read the exact title from the live DB (read-only select on products.name by id), then verify its answer independently:
    - First choice: a row in data/raw/All_certified_psus.xlsx matching the brand, model and wattage, loaded through import_80plus_efficiency's loader.
    - Otherwise: the manufacturer's own product page stating the 80 PLUS tier. Fetch it read-only and record the URL and the verbatim quote.
    - Never use psu_title_extractions or any stored LLM output as evidence (Pitfall 9).

    Candidates (the trap each one tests):
    - MSI MAG A750GL PCIE5 (id 3960): the "GL" suffix means Gold.
    - MSI MAG A850GL (id 20457 or 17180): the same "GL" inference at a different wattage.
    - MSI MAG A650BN (one of ids 16759, 18924, 14917): the "BN" suffix means Bronze.
    - Corsair RM750E, "Cybenetics Gold" in the title (id 6186): the control where Cybenetics and 80 PLUS agree.
    - Cooler Master MWE Gold 850 (id 16673 or 19204): the tier word is in the model name.
    - Thermaltake Toughpower GF A3 750W (id 15310): an explicit tier. It is a near-duplicate of a few-shot prompt title, so prefer another explicit-tier title if one verifies, and record why in trap.
    - One untiered case, expected None: a catalog PSU title that states no tier, where the registry has no entry for that model AND the manufacturer page shows no 80 PLUS certification. If absence can't be confirmed from both sources, don't include an untiered case.

    Lock only verified cases, each with complete evidence. Aim for 8 total. If fewer verify, set N to the verified count, and update the module docstring and a test asserting len(CASES) == N. Record the reason for each dropped candidate in the SUMMARY.

    Add a test that every case has non-empty title, trap and evidence strings, and that no title is identical to either few-shot title in PSU_IDENTITY_BATCH_PROMPT.

    You may use the free-AI helper only for drafting trap wording. Never use it for answers.
  </action>
  <verify>
    <automated>python -m pytest -q tests/test_benchmark_provider.py</automated>
    <fails_when>non-zero exit, or the summary line reports fewer than 9 passed</fails_when>
  </verify>
  <acceptance_criteria>
    - `python -m pytest -q tests/test_benchmark_provider.py` exits 0, including the evidence-completeness test and the len(CASES) == N test
    - Every CASES entry's evidence names either "80 PLUS registry" with the matched model and level, or a manufacturer URL with a verbatim quote, plus a check date
    - The module docstring states N, and N equals len(CASES)
    - The SUMMARY has a table of every candidate with: id, title, expected, evidence source, and locked or dropped with the reason
  </acceptance_criteria>
  <done>The answer key holds only independently verified answers, and the case count is stated honestly.</done>
</task>

<task type="auto">
  <name>Task 3: Live runs against mistral and google, recorded as-is</name>
  <files>scripts/benchmark_provider.py</files>
  <read_first>
    - scripts/benchmark_provider.py (the file being run)
    - src/services/groq_extraction_service.py (provider_chain docstring: prior 8/8 claims for ministral-14b-latest and gemini-3.1-flash-lite)
  </read_first>
  <action>
    Run the benchmark live with --provider mistral --runs 2, then --provider google --runs 2. Keys come from the local .env through settings. Never read or print them.

    Paste both outputs verbatim into the SUMMARY. ROADMAP Success Criterion 5 expects N/N for both. If a provider scores below N:
    - do not edit CASES, expected values or scoring to make it pass;
    - record the miss with the model's raw answer for the failing title;
    - flag it for the owner. A miss on the rebuilt set is a real finding about the provider, or about the case, which is then re-verified from the sources and never from the model.

    If a provider is unavailable (quota or 5xx), record that and retry once later in the session. This task changes scripts/benchmark_provider.py only if a genuine bug in the script turns up; describe any such fix in the SUMMARY.
  </action>
  <verify>
    <automated>PYTHONIOENCODING=utf-8 python scripts/benchmark_provider.py --provider mistral --runs 2</automated>
    <fails_when>non-zero exit, or no line starting with "Score:" in the output</fails_when>
    <automated>PYTHONIOENCODING=utf-8 python scripts/benchmark_provider.py --provider google --runs 2</automated>
    <fails_when>non-zero exit, or no line starting with "Score:" in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - Both commands exit 0 and print "Score:", "Titles/min:" and "JSON failures:" lines for each run
    - The SUMMARY contains both outputs verbatim, plus a one-line verdict: "mistral k/N, google k/N; Success Criterion 5 met: yes/no"
    - The SUMMARY records the commit sha that ended Task 2, and `git diff <that-sha>..HEAD -- scripts/benchmark_provider.py` shows no change inside the CASES list
  </acceptance_criteria>
  <done>The benchmark is committed and scored live on the two providers the roadmap names, with the results reported honestly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script -> third-party LLM providers | Titles and API keys go over HTTPS to external services |
| model output -> answer key | The model under test must never define what "correct" means |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-19 | Information Disclosure | API keys in the benchmark CLI | medium | mitigate | Keys only from env or settings via --key-env; no key argument; never printed (Task 1, test asserts no --key flag) |
| T-1-20 | Tampering (integrity of results) | answer key | medium | mitigate | Evidence field required per case, registry or manufacturer sources only, CASES frozen before live runs (Tasks 2, 3) |
| T-1-21 | Tampering | live database | low | mitigate | No db.session import, bare prompt constant only; subprocess test asserts db.session is never loaded (Task 1) |
| T-1-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no packages |
</threat_model>

<verification>
- `python -m pytest -q tests --ignore=tests/test_frontend_e2e.py` exits 0
- Both live runs recorded in the SUMMARY
</verification>

<success_criteria>
AI-01 is delivered: the benchmark is committed, verified and scored. ROADMAP Success Criterion 5 is met if both providers score N/N; otherwise the shortfall is reported, not hidden.
</success_criteria>

## Artifacts this phase produces (this plan's share)

| Symbol / file | Kind | Consumers |
|---|---|---|
| `benchmark_provider.CASES` | verified answer key | Phase 8 (AI-02 gate) |
| `benchmark_provider.normalize_rating`, `score_results`, `run_benchmark`, `main` | functions / CLI | owner, Phase 8 trial |
| `tests/test_benchmark_provider.py` | test | phase gate |

## Flagged assumptions

- **AI-01 "8-case" vs verified count:** Success Criterion 5 says "8/8". If fewer than 8 cases verify, the criterion is read as N/N on the verified set, and the owner is told N.
- **Comparability with the 2026-09-20 numbers:** The rebuilt set is not the original. Scores here can't be compared one-for-one with the provider_chain docstring's historical 8/8 claims.

<output>
Create `.planning/phases/01-launch-hardening/01-06-SUMMARY.md` when done.
</output>
