---
phase: 01-launch-hardening
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
owner: web-dev
files_modified:
  - src/api/main.py
  - src/static/404.html
  - src/static/500.html
  - src/static/about.html
  - src/static/privacy.html
  - src/static/favicon.svg
  - src/static/index.html
  - tests/test_error_pages.py
  - tests/test_seo_basics.py
  - tests/test_static_pages.py
autonomous: true
requirements: [WEB-01, WEB-02, WEB-05, SEO-01]
user_setup: []

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "GET /no-such-page returns status 404 with the branded HTML page (content-type text/html, contains a link to /) (WEB-01)"
    - "GET /api/v1/no-such-thing returns status 404 with a JSON body that has a detail key (WEB-01)"
    - "Existing API 404s such as GET /api/v1/builder/builds/<unknown token> stay JSON (WEB-01 regression guard)"
    - "An exception raised inside a page route returns status 500 with the branded 500 HTML; one raised inside an /api/ route returns 500 JSON {\"detail\": \"Internal server error.\"}; neither body contains 'Traceback', 'File \"', the exception's message, 'starlette' or 'fastapi' (WEB-02)"
    - "The 404 and 500 pages carry the same security headers as every other response (SEC-05 continuity)"
    - "GET / HTML has a unique <title>, a meta description, og:title, og:description and og:type tags, a favicon link and a canonical link (SEO-01)"
    - "GET /robots.txt and GET /sitemap.xml return 200; the sitemap is valid XML whose <loc> values are absolute URLs (SEO-01)"
    - "The home page footer links to /about and /privacy; /about says prices are collected automatically and may lag, that we are not affiliated with any retailer, that shoppers should verify with the retailer before buying, and shows the retailer contact block (WEB-05)"
    - "With CONTACT_EMAIL empty, /about shows a 'contact address coming soon' note and contains no email address; with CONTACT_EMAIL set it shows a mailto link to exactly that address (WEB-05, owner decision #6 still open)"
    - statement: "[EDGE WEB-01/concurrency] The 404/500 handlers are stateless; concurrent bad requests each get their own correct response"
      verification: backstop
  prohibitions:
    - requirement_id: WEB-05
      category: values
      status: resolved
      verification: test
      flagged: true
      statement: "The About page must never show an invented or example contact address; until the owner sets CONTACT_EMAIL it says the address is coming"
    - requirement_id: WEB-05
      category: values
      status: resolved
      verification: judgment
      flagged: true
      statement: "The Privacy page must describe what the site actually does (checked against app.js and the server), including third-party font requests to Google Fonts; no boilerplate claims the code doesn't back up"
    - requirement_id: WEB-02
      category: safety
      status: resolved
      verification: test
      flagged: true
      statement: "Error responses must never reveal stack traces, exception text, file paths or framework names to the visitor"
  artifacts:
    - path: src/static/404.html
      provides: "branded not-found page"
    - path: src/static/500.html
      provides: "branded server-error page with no dynamic content"
    - path: src/static/about.html
      provides: "About/Disclaimer copy with a {{CONTACT}} token"
      contains: "{{CONTACT}}"
    - path: src/static/privacy.html
      provides: "Privacy page"
    - path: src/api/main.py
      provides: "branded_404/branded_500 handlers, /robots.txt, /sitemap.xml, /about, /privacy routes"
      contains: "def robots_txt"
  key_links:
    - from: src/api/main.py
      to: src/static/404.html
      via: "StarletteHTTPException handler returns FileResponse(static_dir / '404.html', status_code=404) for non-/api/ paths"
      pattern: "404.html"
    - from: src/api/main.py
      to: src/configs/settings.py
      via: "/about route reads settings.CONTACT_EMAIL at request time and substitutes {{CONTACT}}"
      pattern: "CONTACT_EMAIL"
    - from: src/static/index.html
      to: /about and /privacy routes
      via: "footer anchor tags"
      pattern: "href=\"/about\""
---

<objective>
Make the site's edges presentable and honest: branded 404 and 500 pages with no traceback, SEO basics on the home page plus robots.txt and a sitemap, and About/Disclaimer and Privacy pages linked from the footer.

Purpose: ROADMAP Phase 1 Success Criterion 3 ("a bad URL shows a branded 404; a server error shows no traceback") and the public-trust copy WEB-05 requires before launch.
Output: four new static pages, a favicon, handlers and routes in main.py, three new test files.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-launch-hardening/01-RESEARCH.md
@.planning/phases/01-launch-hardening/01-PATTERNS.md
@.planning/phases/01-launch-hardening/01-01-SUMMARY.md
@.claude/agents/web-dev.md
@src/api/main.py
@src/static/index.html

This plan uses 01-01's api.main.apply_security_headers() and settings.CONTACT_EMAIL, which is why it follows 01-01 (it also edits main.py).
Locked decisions: no price alerts or notifications, so the pages must not offer any sign-up or alert. The contact address is undecided (owner decision #6): use the config placeholder, never a hardcoded address.
.gitignore ignores *.html except src/static/*.html, so new pages must live directly in src/static/ to be committed.
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): Branded 404 and traceback-free 500, from handler to rendered page</name>
  <files>src/api/main.py, src/static/404.html, src/static/500.html, tests/test_error_pages.py</files>
  <read_first>
    - src/api/main.py (the file being modified; static_dir, serve_index FileResponse pattern, apply_security_headers from 01-01)
    - src/static/index.html (lines 1-10 head block and the footer, to copy the look)
    - .planning/phases/01-launch-hardening/01-RESEARCH.md (Pattern 4: what already works and the handler shapes)
    - .planning/phases/01-launch-hardening/01-PATTERNS.md (sections on the 404/500 handlers and static pages)
  </read_first>
  <behavior>
    - GET /no-such-page gives 404, text/html, and a body containing href="/"
    - GET /api/v1/no-such-thing gives 404, application/json, and a detail key
    - GET /api/v1/builder/builds/zzzz-unknown gives 404 JSON; a stub get_db via app.dependency_overrides returns no row, so the DB isn't touched
    - A test-only page route that raises RuntimeError("secret-boom") gives 500 HTML without "Traceback", "secret-boom", 'File "', "starlette" or "fastapi"
    - A test-only /api/ route that raises gives 500 JSON {"detail": "Internal server error."}
    - The 404 and 500 responses carry x-content-type-options and content-security-policy
  </behavior>
  <action>
    Tests first (see them fail).

    In src/api/main.py, per WEB-01 and WEB-02, follow RESEARCH Pattern 4:
    - Register an exception handler for starlette.exceptions.HTTPException. For status 404 on a path that does not start with /api/, it returns FileResponse(static_dir / "404.html", status_code=404). Otherwise it delegates to fastapi.exception_handlers.http_exception_handler, keeping JSON for /api/* and every other status.
    - Register an exception handler for Exception. For /api/ paths it returns JSONResponse({"detail": "Internal server error."}, status_code=500). Otherwise it returns FileResponse(static_dir / "500.html", status_code=500).
    - Pass both handler responses through apply_security_headers(): Starlette's server-error layer is outside user middleware, so the 500 path would otherwise lose the SEC-05 headers.
    - Log the exception server-side with the project logger (common.logger.get_logger) so it isn't lost. Never put it in the response.
    - Leave the FastAPI debug flag at its default (False).

    Create src/static/404.html and src/static/500.html as plain, self-contained pages:
    - The same head shape as index.html (charset, viewport, a page-specific title, stylesheet /static/style.css with the same ?v= query).
    - A short plain-English message, and a link back to "/".
    - No script tags and no inline event handlers.
    - 500.html must not depend on the API or the database.

    The test adds throwaway routes with app.add_api_route in a fixture and removes them after. It uses TestClient(app, raise_server_exceptions=False) so the 500 path is exercised as a real client sees it.
  </action>
  <verify>
    <automated>python -m pytest -q tests/test_error_pages.py</automated>
    <fails_when>non-zero exit, or the summary line reports fewer than 6 passed</fails_when>
  </verify>
  <acceptance_criteria>
    - `python -m pytest -q tests/test_error_pages.py` exits 0 with at least 6 passed
    - `git check-ignore src/static/404.html src/static/500.html src/static/about.html src/static/privacy.html src/static/favicon.svg` prints nothing, so .gitignore won't hide any new page
    - `git grep -n "<script" -- src/static/404.html src/static/500.html` prints nothing
    - `git grep -n "404.html" -- src/api/main.py` prints at least one line
  </acceptance_criteria>
  <done>Typos land on a branded page, API typos stay JSON, and a crash shows a generic page or JSON with no internals.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SEO basics: head tags, favicon, robots.txt, sitemap.xml</name>
  <files>src/static/index.html, src/static/favicon.svg, src/api/main.py, tests/test_seo_basics.py</files>
  <read_first>
    - src/static/index.html (the file being modified; the 8-line head block)
    - src/api/main.py (the file being modified; route registration style, include_in_schema=False as on serve_index)
    - .planning/phases/01-launch-hardening/01-RESEARCH.md (Code Example "Minimal robots.txt / sitemap.xml stub")
  </read_first>
  <behavior>
    - GET / HTML has exactly one <title>, containing "PC Builder"
    - GET / HTML has meta name="description" with non-empty content
    - GET / HTML has meta property og:title, og:description and og:type
    - GET / HTML has link rel="icon" pointing at /static/favicon.svg, and GET /static/favicon.svg returns 200 image/svg+xml
    - GET / HTML has link rel="canonical"
    - GET /robots.txt returns 200 text/plain containing "User-agent: *", "Disallow: /api/" and a "Sitemap:" line with an absolute http(s) URL
    - GET /sitemap.xml returns 200 with an XML content type; xml.etree parses it; every <loc> starts with http:// or https://; it lists /, /about and /privacy
  </behavior>
  <action>
    Tests first.

    SEO-01 changes to the head of src/static/index.html. Add these tags to the existing block rather than rewriting it:
    - meta name="description": one sentence on comparing PC part prices across 10 Indian retailers and checking build compatibility.
    - og:title, og:description, og:type=website.
    - link rel="icon" type="image/svg+xml" href="/static/favicon.svg".
    - link rel="canonical" href="/".

    The canonical link is relative because no domain exists yet. The SUMMARY must note that Phase 3 (03-02) switches it, and adds og:url, to the absolute domain. Keep the existing title text.

    Create src/static/favicon.svg: a small, valid, self-contained SVG with no external references.

    In src/api/main.py add these routes, each with include_in_schema=False:
    - GET /robots.txt: a PlainTextResponse with "User-agent: *", "Allow: /", "Disallow: /api/" and "Sitemap: " followed by the absolute sitemap URL, built from str(request.base_url).
    - GET /sitemap.xml: a Response with media_type "application/xml". It is a urlset in the sitemaps.org 0.9 namespace with absolute <loc> entries for "", "about" and "privacy", joined onto request.base_url.

    Per-model entries are SEO-04 (Phase 5) and out of scope. Note in the SUMMARY that behind the Phase 3 proxy, request.base_url needs uvicorn proxy headers to report https and the real host.
  </action>
  <verify>
    <automated>python -m pytest -q tests/test_seo_basics.py</automated>
    <fails_when>non-zero exit, or the summary line reports fewer than 7 passed</fails_when>
  </verify>
  <acceptance_criteria>
    - `python -m pytest -q tests/test_seo_basics.py` exits 0 with at least 7 passed
    - `git grep -n 'rel="canonical"' -- src/static/index.html` prints one line
    - `git grep -n 'property="og:title"' -- src/static/index.html` prints one line
    - `git grep -n "def robots_txt" -- src/api/main.py` prints one line
    - `git grep -n "def sitemap_xml" -- src/api/main.py` prints one line
  </acceptance_criteria>
  <done>The home page has search and link-preview metadata and a favicon, and crawlers get a robots.txt and a valid sitemap.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: About/Disclaimer and Privacy pages, footer links, contact placeholder</name>
  <files>src/static/about.html, src/static/privacy.html, src/static/index.html, src/api/main.py, tests/test_static_pages.py</files>
  <read_first>
    - src/static/index.html (the file being modified; the site-footer block and its footer-note copy)
    - src/api/main.py (the file being modified)
    - src/configs/settings.py (CONTACT_EMAIL added by 01-01)
    - src/static/app.js (grep it for localStorage, sessionStorage, document.cookie and any third-party URL, so the Privacy page states only what's true)
    - src/api/routes/builder.py (save_build: what a saved build stores and how the share token is made)
    - .planning/REQUIREMENTS.md (WEB-05 exact content list)
  </read_first>
  <behavior>
    - GET / contains href="/about" and href="/privacy" inside the site footer
    - GET /about returns 200 HTML containing the phrases "collected automatically", "may lag", "not affiliated", "verify" and "retailer"
    - With settings.CONTACT_EMAIL = "" (monkeypatched), GET /about contains "coming soon" and no "@" character in the contact block, and the {{CONTACT}} token is gone
    - With settings.CONTACT_EMAIL = "hello@test.invalid" (monkeypatched), GET /about contains mailto:hello@test.invalid
    - A CONTACT_EMAIL containing "<b>" is HTML-escaped in the output
    - GET /privacy returns 200 HTML that mentions saved builds and Google Fonts
    - /about and /privacy both link back to "/" and to each other
  </behavior>
  <action>
    Tests first.

    Create src/static/about.html. It uses the same head shape as index.html. Its plain-English copy covers everything WEB-05 requires:
    - Prices are collected automatically from each retailer's public pages, and may lag behind the shop.
    - We are not affiliated with any retailer, and link to each shop's own page.
    - Always verify the price, stock and specs with the retailer before buying.
    - Compatibility checks say "unverified" when a spec isn't published.
    - A "For retailers" section with the contact block. This section contains the literal token {{CONTACT}} exactly once.

    Create src/static/privacy.html. Describe only what the code does, after checking app.js and builder.py:
    - No user accounts.
    - Saved builds store only the chosen part ids, an optional name and optional notes, under a random share token.
    - Whether any browser storage is used, as found by the grep.
    - Pages load fonts from Google Fonts, so Google receives the visitor's IP address.
    - Product images are proxied through our server.
    - Standard server logs.
    - No analytics or ad trackers, unless the grep finds some; if it does, list them.
    - No price alerts or email sign-ups (owner decision).

    In src/api/main.py add GET /about and GET /privacy (include_in_schema=False). /privacy returns FileResponse(static_dir / "privacy.html"). /about reads about.html and replaces {{CONTACT}} with:
    - when settings.CONTACT_EMAIL is non-empty: an anchor with mailto:, the address, and html.escape applied to both;
    - otherwise: the text "A dedicated contact address for retailers is coming soon."

    It reads settings.CONTACT_EMAIL at request time, not import time, so the owner's later .env change needs only a restart and tests can monkeypatch it. Per WEB-05 and owner decision #6 (contact still open), never hardcode an address.

    In the index.html footer, add a small nav inside the existing footer-inner block with links to /about ("About &amp; disclaimer") and /privacy ("Privacy"). Keep the existing footer-note and the stores-grid unchanged: test_footer_lists_every_active_retailer depends on them. Add the same two links to the about and privacy pages.
  </action>
  <verify>
    <automated>python -m pytest -q tests/test_static_pages.py</automated>
    <fails_when>non-zero exit, or the summary line reports fewer than 7 passed</fails_when>
    <automated>python -m pytest -q tests/test_frontend_e2e.py -k "TestPageLoads or footer"</automated>
    <fails_when>non-zero exit, any test reported as skipped, or fewer than 4 passed</fails_when>
  </verify>
  <acceptance_criteria>
    - `python -m pytest -q tests/test_static_pages.py` exits 0
    - `git grep -c "{{CONTACT}}" -- src/static/about.html` prints 1
    - `git grep -nE "[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[a-z]{2,}" -- src/static/about.html src/static/privacy.html src/api/main.py` prints nothing (no hardcoded address anywhere)
    - `git grep -n 'href="/privacy"' -- src/static/index.html` prints one line
    - `python -m pytest -q tests --ignore=tests/test_frontend_e2e.py` exits 0
  </acceptance_criteria>
  <done>The footer links to About/Disclaimer and Privacy, the pages carry the required disclosures, and the contact block is a config-driven placeholder until the owner picks an address.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server internals -> visitor | Exceptions and framework details must not cross into responses |
| config -> HTML | CONTACT_EMAIL is inserted into HTML at request time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-06 | Information Disclosure | Exception handler in src/api/main.py | medium | mitigate | Generic 500 HTML/JSON, exception logged server-side only; test asserts no traceback, message, file path or framework name (Task 1) |
| T-1-07 | Tampering (injection) | /about CONTACT_EMAIL substitution | low | mitigate | html.escape on the address and the mailto href; test with a markup-bearing value (Task 3) |
| T-1-08 | Information Disclosure | Privacy page accuracy | low | mitigate | Copy written only after grepping app.js/builder.py for storage, cookies and third parties; Google Fonts disclosed (Task 3) |
| T-1-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no packages |
</threat_model>

<verification>
- `python -m pytest -q tests/test_error_pages.py tests/test_seo_basics.py tests/test_static_pages.py` exits 0
- `python -m pytest -q tests --ignore=tests/test_frontend_e2e.py` exits 0
- `python -m pytest -q tests/test_frontend_e2e.py -k "TestPageLoads or footer"` exits 0 with no skips
</verification>

<success_criteria>
ROADMAP Phase 1 Success Criterion 3 is met (branded 404, no traceback on 500). WEB-01, WEB-02, WEB-05 and SEO-01 are green by test.
</success_criteria>

## Artifacts this phase produces (this plan's share)

| Symbol / file | Kind | Consumers |
|---|---|---|
| `api.main` HTTPException handler (branded 404) | exception handler | all non-API 404s |
| `api.main` Exception handler (generic 500) | exception handler | all unhandled errors |
| `api.main.robots_txt`, `api.main.sitemap_xml` | routes | crawlers; SEO-04 (Phase 5) extends the sitemap |
| `api.main` /about and /privacy routes | routes | footer links |
| `src/static/404.html`, `500.html`, `about.html`, `privacy.html`, `favicon.svg` | static files | visitors |
| `tests/test_error_pages.py`, `tests/test_seo_basics.py`, `tests/test_static_pages.py` | tests | phase gate |

## Flagged assumptions (edge probe: unclassified rows, never silently dropped)

- **WEB-02 (unclassified):** Assumed "generic" means a fixed page or JSON body with no dynamic text at all. The exception is logged server-side. Sentry reporting is OPS-08 (Phase 3).
- **WEB-05 (unclassified):** VALIDATION.md suggested a Playwright `-k footer` test. This plan uses a TestClient test on the served HTML instead: the same assertions, faster. The existing Playwright footer test still runs.
- **SEO-01 (unclassified):** The canonical link and sitemap hosts are relative to the request until a domain exists. Phase 3 (03-02) must set absolute URLs and turn on uvicorn proxy headers. og:image is not set this phase (no share image asset). SEO-05 adds per-build OG tags in Phase 5.

<output>
Create `.planning/phases/01-launch-hardening/01-02-SUMMARY.md` when done.
</output>
