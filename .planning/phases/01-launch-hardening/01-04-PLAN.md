---
phase: 01-launch-hardening
plan: 04
type: execute
wave: 1
depends_on: []
owner: data-engineer
files_modified:
  - dags/scheduled_scraper_dag.py
  - tests/test_price_freshness.py
  - scripts/measure_db_growth.py
  - tests/test_db_growth.py
autonomous: true
requirements: [OPS-05, OPS-06, OPS-07]
user_setup: []

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "When any active store has saved no price in 24 hours, check_price_freshness raises RuntimeError whose message names every stale store (display name and last-saved time, or 'never'); fresh stores are not named (OPS-05)"
    - "When the canonical-extraction stage made 0 extractions in a cycle while the backlog was above 0, the Airflow extraction task fails with a RuntimeError naming the backlog size (OPS-06)"
    - "stale_stores, latest_price_by_store, check_price_freshness, backlog_snapshot, count_extraction_progress, check_extraction_progress and execute_canonical_extraction_checked are defined at module level outside the Airflow import guard, so the Phase 2 worker can import them with no Airflow installed"
    - "scripts/measure_db_growth.py runs inside a READ ONLY transaction, writes nothing, and prints DB size, the largest tables, price_history rows per day (7- and 30-day averages) and a 12-month projection for keep-everything vs 90-day raw plus daily low/high, against the 150 GB volume and the 20 GB / 10 GB object-storage tiers (OPS-07)"
    - "[EDGE OPS-05/boundary] A store whose newest price is exactly 24h old is fresh; 24h plus 1 second is stale; a store with no price ever (None) is stale"
    - "[EDGE OPS-05/precision] tz-aware and naive timestamps are compared consistently: 'now' takes the tzinfo of the value being compared, as the current code does"
    - "[EDGE OPS-05/idempotency] Running check_price_freshness twice with no new data gives the same result and writes nothing"
    - "[EDGE OPS-06/adjacency] backlog_before=5 with progress=0 fails; with progress=1 it passes"
    - "[EDGE OPS-06/empty] backlog_before=0 passes even with 0 extractions (nothing to do is not a failure); an empty before-snapshot gives progress 0"
    - statement: "[EDGE OPS-06/ordering] Products that join the backlog during the cycle (new scrapes) neither mask nor trigger the check, because progress is counted only over the before-snapshot product ids"
      verification: backstop
    - statement: "[EDGE OPS-05/concurrency] The freshness check only reads, so overlapping DAG runs can't corrupt state; each run reports what it saw"
      verification: backstop
  prohibitions:
    - requirement_id: OPS-05
      category: values
      status: resolved
      verification: test
      flagged: true
      statement: "The freshness and backlog checks fail the task loudly and nothing more; they must never send email, push or any other notification (owner decision: no alerts or notification channels)"
    - requirement_id: OPS-07
      category: safety
      status: resolved
      verification: test
      flagged: true
      statement: "The DB measurement must never write to, lock or vacuum the live database; it runs in a read-only transaction"
  artifacts:
    - path: dags/scheduled_scraper_dag.py
      provides: "per-store freshness check and the extraction-progress check as plain functions, wired into the DAG"
      contains: "def check_extraction_progress"
    - path: scripts/measure_db_growth.py
      provides: "read-only size and growth report with a 12-month projection"
      contains: "READ ONLY"
    - path: tests/test_price_freshness.py
      provides: "pure-function tests for OPS-05/OPS-06"
    - path: tests/test_db_growth.py
      provides: "projection math tests"
  key_links:
    - from: dags/scheduled_scraper_dag.py (canonical_extraction_task)
      to: execute_canonical_extraction_checked
      via: "PythonOperator python_callable"
      pattern: "python_callable=execute_canonical_extraction_checked"
    - from: check_price_freshness
      to: stale_stores
      via: "latest_price_by_store(session) result passed to the pure function"
      pattern: "stale_stores\\("
---

<objective>
Make pipeline failures loud and specific, and measure the database. The freshness check names each store that stopped saving prices. The extraction stage fails when every provider is exhausted with work still waiting. A read-only script measures DB size and growth so Phase 3 can size retention and backups.

Purpose: ROADMAP Phase 1 Success Criterion 4, and the STATE.md blocker "DB size is unmeasured, so retention and backup sizing depend on 01-04".
Output: DAG changes, extended freshness tests, the new measure_db_growth.py and its test.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-launch-hardening/01-RESEARCH.md
@.planning/phases/01-launch-hardening/01-PATTERNS.md
@.claude/agents/data-engineer.md
@dags/scheduled_scraper_dag.py
@tests/test_price_freshness.py

Locked decisions:
- No Airflow in production; Phase 2's worker calls these same functions. Every new function lives outside the "try: from airflow import DAG" guard.
- No alerts or notifications. The checks fail tasks; they don't message anyone.
- Tests never write to the live DB. The only live-DB access in this plan is read-only (the freshness query and the measurement script).

Runs in parallel with web-dev's 01-01..01-03 and 01-05, and with 01-06. No shared files.
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): Per-store freshness, from the SQL query to the named failure message</name>
  <files>dags/scheduled_scraper_dag.py, tests/test_price_freshness.py</files>
  <read_first>
    - dags/scheduled_scraper_dag.py (the file being modified; price_data_is_stale, check_price_freshness, the import guard and task wiring at the bottom)
    - tests/test_price_freshness.py (the file being modified; the sys.path plus module import pattern)
    - src/db/models/store.py, src/db/models/price_history.py, src/db/models/product.py (column names: Store.active, Store.display_name, PriceHistory.product_id, PriceHistory.scraped_at, Product.sid)
    - .planning/phases/01-launch-hardening/01-RESEARCH.md (Pattern 7)
  </read_first>
  <behavior>
    - stale_stores({"A": now - 2h, "B": now - 25h, "C": None}, now) returns ["B", "C"] (sorted by name)
    - Exactly 24h counts as fresh; 24h plus 1s counts as stale
    - An empty dict gives []
    - A tz-aware latest with a naive now is handled without TypeError (now is made to match the value's tzinfo)
    - format_stale_message([("B", dt), ("C", None)]) contains "B", "C" and "never"
    - The existing three price_data_is_stale tests still pass unchanged
  </behavior>
  <action>
    Tests first (pure functions only, no DB).

    In dags/scheduled_scraper_dag.py, at module level near price_data_is_stale, per OPS-05:
    - Add stale_stores(latest_by_store: dict[str, datetime | None], now: datetime, max_age: timedelta = PRICE_MAX_AGE) -> list[str]. It reuses price_data_is_stale per store, making now tz-consistent with each value exactly as the current check_price_freshness does, and returns names sorted.
    - Add format_stale_message(stale: list[tuple[str, datetime | None]]) -> str. It builds text like "No price saved in 24h for: B (last: 2026-09-23 10:00), C (last: never)".
    - Add latest_price_by_store(session) -> dict[str, datetime | None]. It is one query: active stores, left-outer-joined to Product on Product.sid == Store.id and to PriceHistory on PriceHistory.product_id == Product.id, grouped by store, selecting max(PriceHistory.scraped_at). A store with zero rows appears with None. Read only.
    - Rewrite check_price_freshness() to call these three and raise RuntimeError(format_stale_message(...)) when any store is stale. Otherwise print one "[Freshness]" line per store.

    Keep the function name and its task wiring unchanged, so the DAG's freshness_task needs no edit. Import sqlalchemy and the models inside functions, as the current code does. After tests pass, run a read-only smoke from the repo root to check the live query returns one entry per active store. Record the output in the SUMMARY.
  </action>
  <verify>
    <automated>python -m pytest -q tests/test_price_freshness.py</automated>
    <fails_when>non-zero exit, or the summary line reports fewer than 8 passed</fails_when>
    <automated>PYTHONIOENCODING=utf-8 python -c "import sys; sys.path[:0]=['src','dags']; import scheduled_scraper_dag as d; from db.session import SessionLocal; s=SessionLocal(); r=d.latest_price_by_store(s); s.close(); print(len(r)); print(r)"</automated>
    <fails_when>non-zero exit, a traceback, or the first printed line is 0</fails_when>
  </verify>
  <acceptance_criteria>
    - `python -m pytest -q tests/test_price_freshness.py` exits 0
    - `git grep -n "^def stale_stores" -- dags/scheduled_scraper_dag.py` prints one line (module level, outside the guard)
    - `git grep -n "^def latest_price_by_store" -- dags/scheduled_scraper_dag.py` prints one line
    - The read-only smoke prints a count equal to the number of active stores (10 expected), recorded in the SUMMARY
  </acceptance_criteria>
  <done>A single silent store now fails the freshness task with its name in the message.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Loud failure when a cycle makes 0 extractions with a backlog</name>
  <files>dags/scheduled_scraper_dag.py, tests/test_price_freshness.py</files>
  <read_first>
    - dags/scheduled_scraper_dag.py (the file being modified; execute_canonical_extraction with its runners list and the try/except that swallows errors; the PythonOperator wiring)
    - scripts/extract_gpu_titles_groq.py (eligibility filter: p_category plus canonical_id IS NULL; how spec_status is set on success and on batch failure)
    - scripts/extract_ram_titles_groq.py and scripts/wire_ram_canonical.py (how RAM leaves the backlog, which may differ)
    - src/db/models/product.py (spec_status values: pending, extracted, needs_review, failed)
    - .planning/phases/01-launch-hardening/01-RESEARCH.md (Pattern 7, OPS-06 part)
  </read_first>
  <behavior>
    - count_extraction_progress(before={1: (None, "pending"), 2: (None, "pending")}, after={1: ("k1", "extracted"), 2: (None, "pending")}) == 1
    - A row that only moves to spec_status "failed" does not count as progress
    - A row whose canonical_id becomes set counts even if its status is unchanged
    - An empty before gives 0
    - check_extraction_progress(5, 0) raises RuntimeError whose message contains "5"
    - check_extraction_progress(5, 1) does not raise
    - check_extraction_progress(0, 0) does not raise
  </behavior>
  <action>
    Tests first. Before writing the query, run a read-only measurement of the current backlog and record it in the SUMMARY: the count of products with canonical_id IS NULL, per category and per spec_status, for the categories the runners cover. It tells the owner whether a permanent residue exists (rows that stay NULL even after a successful extraction). A residue that never clears would make this check fire every cycle, so report it rather than hide it.

    Add at module level, outside the Airflow guard (per OPS-06):
    - BACKLOG_CATEGORIES: the tuple of p_category values the runners handle (CPU, Motherboard, GPU, Storage, Power Supply, CPU Cooler, Cabinet, Monitor, RAM). Confirm each spelling against the extractors' own where-clauses.
    - backlog_snapshot(session) -> dict[int, tuple[str | None, str]]: product id mapped to (canonical_id, spec_status) for products in those categories with canonical_id IS NULL. Read only.
    - state_of(session, ids) -> dict[int, tuple[str | None, str]]: re-reads the same ids.
    - count_extraction_progress(before, after) -> int: counts ids whose canonical_id became non-null, or whose spec_status changed to something other than "failed".
    - check_extraction_progress(backlog_before: int, progress: int) -> None: raises RuntimeError(f"0 extractions this cycle with a backlog of {backlog_before} - every provider may be exhausted.") when backlog_before > 0 and progress == 0.
    - execute_canonical_extraction_checked(limit_per_category: int = 10): opens a SessionLocal for the snapshot, calls the unchanged execute_canonical_extraction, re-reads state_of for the snapshot ids in a fresh session, prints backlog and progress, then calls check_extraction_progress.

    DAG wiring, inside the guard:
    - canonical_extraction_task uses python_callable=execute_canonical_extraction_checked.
    - physical_specs_task gets trigger_rule="all_done". Once extraction fails loudly for provider exhaustion, the spec and policy stages still run on what is already keyed, matching the existing comment's reasoning for all_done.

    Do not thread return values through the nine extractor scripts (RESEARCH says not to).
  </action>
  <verify>
    <automated>python -m pytest -q tests/test_price_freshness.py</automated>
    <fails_when>non-zero exit, or the summary line reports fewer than 15 passed</fails_when>
  </verify>
  <acceptance_criteria>
    - `python -m pytest -q tests/test_price_freshness.py` exits 0
    - `git grep -n "^def check_extraction_progress" -- dags/scheduled_scraper_dag.py` prints one line
    - `git grep -n "python_callable=execute_canonical_extraction_checked" -- dags/scheduled_scraper_dag.py` prints one line
    - `git grep -n -e smtplib -e send_mail -e EmailOperator -e webhook -- dags/scheduled_scraper_dag.py` prints nothing
    - `python -c "import sys; sys.path[:0]=['src','dags']; import scheduled_scraper_dag as d; print(callable(d.check_extraction_progress))"` prints True in an environment without Airflow
    - The SUMMARY contains the backlog-by-category-and-status table
  </acceptance_criteria>
  <done>An exhausted provider chain with work waiting now fails the extraction task loudly, and the backlog's makeup is on record.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Read-only DB size and growth measurement with a 12-month projection</name>
  <files>scripts/measure_db_growth.py, tests/test_db_growth.py</files>
  <read_first>
    - scripts/scrape_cooler_height.py (module docstring, argparse, SessionLocal and "=" * 78 banner conventions)
    - scripts/ask_free_ai.py (stdout utf-8 reconfigure guard)
    - src/db/models/price_history.py (table name and columns)
    - .planning/ROADMAP.md (Phase 3 storage numbers: 150 GB block volume, 20 GB OCI Object Storage, 10 GB R2; OPS-07 retention policy: 90 days raw, then daily low/high)
    - .planning/phases/01-launch-hardening/01-RESEARCH.md (live baseline: 127 MB, 386,301 price_history rows over 59 days)
  </read_first>
  <behavior>
    - project_growth(current_bytes=100, bytes_per_row=10, rows_per_day=5, days=365, retention_days=None, rollup_rows_per_day=0) == 100 + 10*5*365
    - With retention_days=90 and rollup_rows_per_day=2, the raw part is capped at 90 days of rows plus 2 rollup rows per day for the rest
    - fits(projected_bytes, budget_bytes) is True when equal, False when one byte over
    - rows_per_day == 0 returns current_bytes unchanged
  </behavior>
  <action>
    Tests first for the pure math.

    Create scripts/measure_db_growth.py. Its docstring states "Read-only. Never writes." and it has no --apply flag. Per OPS-07, it does the following.
    1. Opens SessionLocal and, before any query, runs SET TRANSACTION READ ONLY, so a write would error.
    2. Queries:
       - pg_database_size(current_database());
       - the 10 largest tables by pg_total_relation_size;
       - price_history total rows, min/max scraped_at, and rows per day averaged over the last 7 and last 30 days;
       - bytes per price_history row (total relation size / rows);
       - distinct products with a price per day over the last 7 days (the size of a daily low/high rollup).
    3. Uses pure functions project_growth(current_bytes, bytes_per_row, rows_per_day, days, retention_days, rollup_rows_per_day) -> int and fits(projected_bytes, budget_bytes) -> bool to print a 12-month projection for "keep everything" vs "90 days raw plus daily low/high". Each is checked against the 150 GB block volume.
    4. Accepts an optional --dump-bytes N, the size of a compressed dump measured by hand, and projects 12-month dump size against the 20 GB (OCI) and 10 GB (R2) budgets. How many copies are retained is decided in 03-03; print the per-copy figure.
    5. Ends the transaction with a rollback.

    Measure the dump by hand, read-only, with: docker exec pc_builder_postgres pg_dump -Fc -U pc_builder pc_builder | wc -c. Then run the script with --dump-bytes set to that number.

    Paste the full output into the SUMMARY with one plain-English line: does retention need to be applied in 03-03, yes or no, and why. Applying retention is 03-03's job, not this plan's.
  </action>
  <verify>
    <automated>python -m pytest -q tests/test_db_growth.py</automated>
    <fails_when>non-zero exit, or the summary line reports fewer than 4 passed</fails_when>
    <automated>PYTHONIOENCODING=utf-8 python scripts/measure_db_growth.py</automated>
    <fails_when>non-zero exit, a traceback, or no line containing "12-month" in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `python -m pytest -q tests/test_db_growth.py` exits 0
    - `git grep -n "READ ONLY" -- scripts/measure_db_growth.py` prints at least one line
    - `git grep -n -e ".add(" -e ".commit(" -e INSERT -e UPDATE -e DELETE -e VACUUM -- scripts/measure_db_growth.py` prints nothing
    - `python scripts/measure_db_growth.py` exits 0 and prints DB size, price_history rows/day, and both 12-month projections
    - The SUMMARY records the output, the measured dump size, and the yes/no retention recommendation for 03-03
  </acceptance_criteria>
  <done>DB size and growth are measured, and Phase 3 has real numbers for retention and backup sizing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| check scripts -> live Postgres | Monitoring code runs against the production-shaped database |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-12 | Tampering | scripts/measure_db_growth.py | medium | mitigate | SET TRANSACTION READ ONLY before any query, rollback at end, grep gate for write verbs (Task 3) |
| T-1-13 | Denial of Service (silent failure) | extraction stage swallowing provider errors | medium | mitigate | check_extraction_progress raises when backlog > 0 and progress == 0 (Task 2); per-store freshness (Task 1) |
| T-1-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no packages |
</threat_model>

<verification>
- `python -m pytest -q tests --ignore=tests/test_frontend_e2e.py` exits 0
- The freshness smoke and measure_db_growth.py outputs are in the SUMMARY
</verification>

<success_criteria>
ROADMAP Phase 1 Success Criterion 4: a check fails, naming the store, when one store saves nothing for 24h. OPS-05 and OPS-06 are green by test. OPS-07 is measured and recorded.
</success_criteria>

## Artifacts this phase produces (this plan's share)

| Symbol / file | Kind | Consumers |
|---|---|---|
| `scheduled_scraper_dag.stale_stores`, `format_stale_message`, `latest_price_by_store` | functions | check_price_freshness; Phase 2 worker; AGENT-12 /health/pipeline |
| `scheduled_scraper_dag.BACKLOG_CATEGORIES`, `backlog_snapshot`, `state_of`, `count_extraction_progress`, `check_extraction_progress`, `execute_canonical_extraction_checked` | constants/functions | DAG; Phase 2 worker (02-03) |
| `scripts/measure_db_growth.py` (`project_growth`, `fits`) | script + functions | 03-03 retention/backup sizing |
| `tests/test_db_growth.py` | test | phase gate |

## Flagged assumptions (edge probe: unclassified rows, never silently dropped)

- **OPS-07 (unclassified):** Phase 1 delivers the measurement and the projection. The requirement's "retention keeps 12 months within the block volume and the backups within the free tiers" is met by applying retention in 03-03 ("applying the OPS-07 rollup if growth needs it"). This plan's recommendation decides whether that's needed.
- **OPS-06 backlog residue:** If Task 2's measurement shows products that stay in the backlog even after a successful extraction, the check could fire every cycle. The SUMMARY must say so, so the owner can decide on an exclusion rule before Phase 2's worker adopts the check.

<output>
Create `.planning/phases/01-launch-hardening/01-04-SUMMARY.md` when done.
</output>
